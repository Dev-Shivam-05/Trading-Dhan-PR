# NSE 9:20 scan logs

Written only by automation — GitHub Actions (`.github/workflows/nse-scan.yml`) and the Windows task on the dev PC.
Nothing here is code. Newest result per runner: `LATEST-<runner>.md`. One line per run: `index-<runner>.csv`.
Per run: `<YYYY-MM-DD>/<HHmmss>-<runner>.md` and `.json` (full result, including why each ranked stock passed or failed).
