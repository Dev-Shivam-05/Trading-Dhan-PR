# PHASES — Dhan Option Chain Terminal

One phase per session. If a phase grows past ~8 files, stop and add a row here instead of
absorbing the work.

| # | Phase | Deliverable | Files | Done when | Status |
|---|---|---|---|---|---|
| P0 | Spike + skeleton | SPIKE-01 resolves the GOLD `UnderlyingScrip`; instrument master downloader + cache; `.env` wiring; Fastify boots; `/api/health` returns the six resolved instruments with their lot sizes | 9 | All six chips resolve to a real `(scrip, seg, lot)` triple, or GOLD is documented as blocked | **done** — GOLD resolved 2026-08-28 (`483079 / MCX_COMM / lot 1`) |
| P1 | Chain pipeline | `ChainPoller` (3000 ms, completion-scheduled), `DeriveEngine` (PRD section 4), `TelemetryBus`, SSE endpoints, `DH-901` / `DH-904` handling | ~8 | 10-minute log with zero sub-3000 ms repeats; derived ATM IV and PCR match a fixture to 2 dp | **done** |
| P2 | Option chain grid | Token sheet as CSS custom properties, grid, header strip, chip rail, expiry select, spot marker, ITM tint, OI bar, update flash | ~8 | Screenshot matches the reference density; row count equals the response strike count | **done** |
| P3 | Latency panel | Three live numbers, stage waterfall, sparkline with p50/p95, percentile row, rate-limit ring, 20-row call log, export | ~6 | Exported CSV percentiles match hand computation; panel updates without re-rendering the grid | **done** |
| P4 | States + polish | All eight component states, expressive error/empty/closed states, keyboard map, both themes, contrast audit, 1024px degrade | ~6 | Twelve screenshots from `ui-contract.md` section 5 captured and reviewed | **done** |
| P5 | Tick feed + live chart | Dhan WebSocket feed, hand-written binary packet parser, per-contract subscriptions built from each snapshot's strike list, 10 Hz tick batching over SSE, live chart strip | ~5 | Subscriptions match the rendered strike list with zero orphans; frame gaps stay under 250 ms | **done** (2026-08-27) |
| P6 | Chart price axis + drawing tools | Draggable price axis (drag to compress/expand the scale, double-click to reset), crosshair readout, and drawing tools - trendline, horizontal price line, ray, rectangle - persisted per (instrument, expiry) | ~5 | A trendline drawn on NIFTY survives an instrument switch and a page reload, and stays anchored to its price/time coordinates after a scale drag | **done** (2026-08-31) — spec `docs/spec/chart-tools-v1.md`, 19/19 checks pass in replay |
| P7 | Peak-OI tracking | Yesterday's per-strike **peak** OI fetched as `max(open_interest)` over yesterday's 1-min candles from `POST /v2/charts/intraday` (`"oi": true`, `NSE_FNO`), cached per (contract, date); grid column and filter comparing live OI against that peak rather than `previous_oi` (yesterday's close); breach highlight on cross | ~6 | For one strike, the fetched peak equals the hand-computed max of that day's candle OI series, and a live OI above it flags within one poll | **done (replay)** 2026-08-31 — spec `docs/spec/peak-oi-v1.md`, 22 rows, 23/23 checks pass. **The live path has never run**: the plan is inactive, so every number was measured against synthetic candles |
| P8 | 9:20 F&O scanner | Manually triggered scanner over the 210-stock NSE F&O universe: one `POST /v2/marketfeed/quote` carrying all 210 equities **and** their 210 near-month futures, then the three filters - top 50 gainers + top 50 losers, `\|LTP change\| >= 2%`, `\|OI change\| >= 7%` vs the previous session's closing futures OI - printed as two counted lists with the full funnel `210 -> 100 -> n -> m` | ~7 | The seeded replay returns exactly 6 stocks (4 long, 2 short) inside 120 s, and every one independently satisfies all three filters when recomputed by hand from the same payload | **done (replay)** 2026-09-01 - spec `docs/spec/scanner-v1.md`, 20 rows, 32/33 checks pass. **AC5 (chain cadence under scan load) is not measurable on a closed market and stays unverified**; the live path has never run |
| P9 | Option candle colouring | Per-candle OHLC + volume + `open_interest` for one chosen option contract from `/v2/charts/intraday`; candles green/red normally, recoloured **blue** when `vol >= 3x median20` **and** `\|dOI\| >= 5% of prior candle OI` with `dOI > 0` (big player entering), **yellow** on the same test with `dOI < 0` (exiting), so the move is visible on the option's own chart, not the spot chart | ~8 | The seeded replay day renders exactly 3 blue and 2 yellow candles, and for one of them the tooltip's `vol / median20 / dOI / oi[i-1]` recomputed by hand from the payload yield that same colour | **done (replay)** 2026-09-01 - spec `docs/spec/option-candles-v1.md`, 22 rows, **22/22 checks pass**. Row 14's 60 s refresh measured at +60,016 ms. **The live path has never run** |
| P10a | Terminal shell + chain | Three stacked resizable panes (chart / chain / drawer slot) with 1px seams and persisted sizes, the strike spine pinned with `position:sticky;left:0;right:0` so it cannot scroll out of view, greeks hidden behind a `G` toggle so the default 17 columns fit 1440px with no horizontal scroll, and dark as the default theme. The `.lat` right dock is left working, untouched, inside the new shell | 5 | `table.oc` `scrollWidth === clientWidth` at 1440px with greeks off; `td.spine` stays inside the viewport at every `scrollLeft` with greeks on at 1024px; splitter drag of −60px moves `#chartBody` by 60±2px and survives a reload; row count still equals the response strike count with zero `.hidden` rows | **done (replay)** 2026-09-03 — spec locked with one `go`; **41/41** acceptance checks at 1440x900 and 1024x800, zero console errors. Amendment rows 23, 24. Branch `p10a-terminal-shell` |
| P10b | Latency in its new form | The 380px right dock **deleted**. A 26px always-on status rail carries conn · mode · RTT · age · next-poll ring · OK% · feed · clock; the P3 waterfall, sparkline, percentiles and 20-row call log move to a **horizontal four-column drawer**, closed by default, toggled with `L`. Renderers move to a new `public/telemetry.js` with the same element ids | 5 | With the drawer closed the three big numbers are readable in the rail and `#gridScroll.clientWidth` equals the P10a no-drawer baseline **to the pixel**; opening the drawer changes only `clientHeight`; `/api/telemetry.csv` percentiles still match hand computation; zero console errors over 60 s | **done (replay)** 2026-09-03 — **30/30** acceptance checks, three consecutive runs, zero console errors over 60 s. Amendment rows 25–27. `docs/shots/` re-baselined once, at the end, per row 22 |
| P11 | Audit fixes | Sixteen defects found by a full read-only audit of `src/server/` and `public/` on 2026-09-03, each reproduced with a runnable script before any fix: the candle colour rule implemented as a division so a zero baseline inverts it; `spot ?? 0` collapsing ATM to the lowest strike and persisting that as the day's IV baseline; an unvalidated `expiry` leaking a `ChainPoller` per distinct value; the spot pill positioned off a `display:none` row; single-letter shortcuts firing on `Ctrl`; three change columns left stale beside a ticking LTP; one `Esc` consumed by two handlers | 11 | Every finding has a script that fails before the fix and passes after, and P10a's 41 checks plus the P2-P9 re-proof still pass | **done (replay)** 2026-09-03 — 16 findings, **all reproduced with a runnable script before any fix and re-run after**. 12 code files, over the ~8 rule and recorded as such. `.cache/bug-evidence-server.js` and `.cache/bug-evidence-client.js` are the evidence; P10a 41/41, P10b 30/30 and the P2–P9 re-proof 37/37 all still pass |
| P12 | Live verification pass | The first run of every path that has never touched Dhan: `npm run check` READY, then one `/v2/charts/intraday` call (response shape, epoch units, and **whether `open_interest` is units or contracts**), one `/v2/marketfeed/quote` with 420 instruments, `npm run feed:probe` for the binary parser, and the P7/P8/P9 screens driven against live data | ~4 | `npm run check` reports READY; the intraday and quote response shapes match `dhan-api-contract.md` or the doc is corrected; the binary parser decodes real bytes; **P8's AC5 is measured with the market open**; P7, P8 and P9 each lose their "the live path has never run" note | **unblocked 2026-09-19** — plan Active, `check` READY, `live:probe` 17/0 fail (P17). Still open: `feed:probe`, P7/P8/P9 driven live, and **P8's AC5 with the market open**. Earlier: blocked — needed a fresh token **and** a paid Data API plan. These are two separate gates that fail differently; `npm run check` prints which one you are looking at. **2026-09-17: still blocked** (token expired 2026-08-28, `DH-901` / `808`), so the parts that need no credentials were done ahead of time. `npm run live:probe` (`scripts/live-probe.ts` + the pure `scripts/live-shape.ts`) answers the intraday and quote questions in five serial calls and saves every raw body to `.cache/live/`. Its 36 verdict branches pass against replay payloads and deliberately broken ones, and its gate exits cleanly on the expired token. **Found in Dhan's docs before any live call:** intraday `fromDate`/`toDate` are documented as `YYYY-MM-DD HH:MM:SS`, but `peakoi.ts` sends date-only. The probe tries the app's form first and says which one Dhan accepts. `dhan-api-contract.md` §2.5 and §2.6 now record both endpoints |
| P12b | Live drive of P7 / P9 / feed (P12 remainder) | Each live path checked against a direct Dhan call and a second, independent implementation. The throwaway scripts are in `.cache/p12b-*.ts` | 4 | The feed parser agrees with REST on real bytes; one live strike's peak equals the max of its candle OI; one live candle's colour is re-derived by hand; P8 driven live; P8's AC5 measured with the market open | **partly done 2026-09-19 (Saturday, market shut).** **Feed:** quote (4), full (8) and OI (5) packets for NIFTY and two options agree **exactly** with `/v2/marketfeed/quote` on LTP, volume, OI and OHL. `LTT` turned out to be IST wall-clock encoded as an epoch (contract §2.4). **P7:** three live peaks matched Dhan's raw 18-Sep candles to the unit and the minute. **Bug found and fixed:** replay and live shared `peak-oi.json`, `iv-baseline.json` and `scan-oi.json`, and a cached value is never re-fetched. All 400 cached 16-Sep peaks were replay's (375 candles, not lot multiples) and Dhan's real values differ. A replay scan today wrote `baselineDate 2026-09-18`, exactly the key Monday's live scan would read. Each mode now has its own file, and it was verified that a replay run leaves the live files' mtimes untouched. **P9:** 488/488 live candles (5m / 1m / 15m, NIFTY 22-Sep) agree with Dhan on OHLC, volume and OI and are coloured the same by a second implementation. **Finding for the user, not changed:** every live blue fires in the first 20 minutes (09:15–09:35), because `median20` at the open reaches back into yesterday's quiet tail. **Open:** P8 live (`/api/scan?source=dhan` returns 409 on a shut session, and `NSE_EQ` `net_change` is 0 after the close, see contract §2.6), and P8's AC5. Both need **Mon 21 Sep 09:15+** |
| P27 | Re-baseline the verification suites | The suites in `.cache/` encoded superseded constants — 17 rows and "ATM ±8" where P23 made the chain 16 rows and "Spot ±8"; a 112px chart default where ui-type-v1 row 6 set 200px; Geist Mono where row 1 set Inter; a 1484px table width where row 4 made cells size to content. 21 red lines across six suites, identical on the pre-P25 build. Every replacement is a value the **current spec states**, or the criterion's own invariant where the spec states no number | 6 suites + 4 code | Every suite green, nothing loosened, and each rewritten row naming the spec row that superseded it | **done** 2026-09-22. p19 **48/62 → 62/62**, p20 **19/29 → 30/30**, p22 20/22 → 23/23, p23 18/19 → 19/19, p10a 32/42 → 42/42, P2–P9 re-proof 36/37 → 37/37. **Two of the red lines were real defects, not stale expectations.** (1) The chart header genuinely wrapped at 1024: twelve controls needing ~1025px in a 992px box put `chartPop` and `chartBtn` on lines two and three and the header stood **76px** instead of 34, eating 42px of chart — P22 claimed to have fixed this and its own suite had reported the 76px ever since. The legend now yields at the same 1180px breakpoint the Style button uses. (2) P26's blip-vs-refusal split dropped underlying-candles-v1 row 21's promise that the error state says when it will retry, so a **refusal** showed `request failed (DH-904)` and nothing else while it was in fact coming back in 60 s; both candle stores have `refused()` now. **Three measurement bugs fixed rather than the claims loosened:** a `toFixed(2)` **string** comparison of two float means flipped at a `.xx5` boundary (hand 24085.3950 vs app 24085.3950 rounding differently) while the drawn point was 0.0098px from true; a check read `#uSpot` (repainted from the feed at 10 Hz) against a window that re-anchors on a 3 s snapshot and called 58 of 60 samples off-centre — `window.__grid.spot()` exposes the snapshot's own spot, without which **P23's rule was not measurable from outside at all**; and a one-line header was asserted as `≤ 28px` when P26's two 30px icons make it 34px, so it now asserts that no child sits a row below another. **Open, for the user:** at 1440×900 in replay only **14 of 16** strike rows fit (15 live); all 16 fit at 960. No spec row ever promised 16 at 900 — the check inferred it — so the suites assert reachability with the spine pinned and print the real count. Shrinking the 200px plot would buy the two rows back, and that plot height is the user's own P22 choice. **Final sweep after every fix, 13 suites run one at a time against the current build: p25 44/44, p19 62/62, p20 30/30, p22 23/23, p23 19/19, p24 errors 0, p10a 42/0, P2–P9 re-proof 37/0, p10b 30/0, p9 22/22, p26 27/27, p25-note 4/4, p25-pop 6/6 — 346 checks, 0 failures, every process exiting 0.** The one note the suites raise themselves: `verify-p26.mjs` still counts 1 pre-existing `/favicon.ico` 404, which has no `STATIC` row and is scored separately rather than folded into a green. |
| P25 | TradingView-style chart navigation | Wheel = time zoom about the cursor (0.85/notch); trackpad pinch the same with the page zoom suppressed; shift / horizontal wheel and a plot drag = pan; alt+wheel and a wheel over the 76px gutter = price zoom (0.9); double-click or `0` = reset; a `⟩` arrow appears the moment the view leaves its default. The price scale auto-fits to the candles **inside** the window. The option window gets the same gestures in **index** space. Spec `docs/spec/chart-nav-v1.md`, 20 rows + amendments 21–28 | 6 code | Every gesture measured through `window.__chart.view()` / `__candles.view()`; a P6 trendline still lands on its own (time, price) anchors after a zoom **and** a pan; paint p95 < 8 ms while panning at 375 candles; no earlier suite loses a check | **done (replay + live)** 2026-09-22. `.cache/p25-verify.js` **44/44** with a 60 s soak, `.cache/p25-live-forming.js` **11/11** against the open MCX session, `.cache/p25-note.js` 4/4, `.cache/p25-pop.js` 6/6 inside a P26 pop-out. Paint p50 1.6 / p95 3.2 ms. **Three defects found by measuring, not reading:** the replay tick feed grew a candle on a SHUT session (one bar 5 h 50 min after the 15:25 close, with an empty 21,000,000 ms stretch in front of it, while `/api/ucandles` had returned 76 clean candles — the client drew the gap); the option window stored its right edge as a **rounded** candle index, so each small drag step rounded back and a slow drag panned nothing; and "at the live edge" measured in **milliseconds** let six notches near the right edge drift 144,450 ms and silently un-pin the chart from live data. **Regression, measured against a pre-P25 worktree on its own replay server (8792) rather than assumed:** p20 19/29, p22 20/22, p23 18/19, p10a 32/10, P2–P9 re-proof 36/1 — **byte-identical check lists on both builds**; p19 **48/62 → 55/62**, seven checks FAIL→PASS and none the other way. Those seven are P19's own AC1/AC3/AC9 — they had been red since the phantom candle appeared, and the amendment-21 fix restored them. The remaining reds encode pre-P22/P23 constants (17 vs 16 rows, "ATM ±8" vs "Spot ±8", a 112px vs 200px chart default) and are identical on both builds |
| P24 | Collapsed chart is findable | Collapsed, `#chartBtn` reads "▸ Show chart" | 2 | Collapsed load shows the label; one click restores the 200px strip; survives reload; `C` collapses | **done (live)** 2026-09-20 |
| P23 | Spot window | 8 strikes below the spot + 8 at or above it (16 rows), chip `Spot ±8 · 16 of N`. Spec `docs/spec/spot-window-v1.md` | 2 | Live NIFTY 23,346.40 shows exactly 22,950 … 23,700 | **done (live + replay)** 2026-09-19. 20/20 live, 19/19 replay |
| P22 | Type, fit, chart height | Inter everywhere, no clipped cells, 200px plot, peak tick, note on the axis, Style label, option window over the other side. Spec `docs/spec/ui-type-v1.md` | 8 | 0 clipped cells, 1 family, 17 rows live, window covers no clicked-side cell | **done (live + replay)** 2026-09-19. 22/22 replay, 23/23 live |
| P21 | Previous close | The header's spot change from the daily close of the previous session | 1 | Five NSE chips equal TradingView | **done (live)** 2026-09-19. GOLD open |
| P20 | ATM ±8 strikes + option chart window | The chain renders only the ATM row and 8 strikes each side (17 rows; a strike search still reaches the whole chain). A CE / PE click opens that contract's P9 candles in a floating, draggable, resizable window (position remembered) instead of taking over the NIFTY strip, which stays on screen with its drawing tools. Spec `docs/spec/strike-window-v1.md`, 16 rows | 4 code | 17 rows = ATM ± 8 × step from the payload; edges; search; 3 blue / 2 yellow in the window; strip + drawing while open; Esc / × / chip switch close; position survives reload; 60 s soak | **done (replay)** 2026-09-19. `.cache/p20-verify.js` **29/29**, 60 s soak with zero console errors. Superseded checks rewritten, not dropped: P10a 42/42, P2–P9 re-proof 37/37, P9 22/22, P19 62/62, P10b 30/30, P16 35/37 (the 2 reds are the environmental live-NSE funnel, identical before P19). **Found on screen:** the default window spot hides the PE columns of the lower rows, ATM included, so a better default waits on the user. AC2 (the window following a moving ATM) is proven only indirectly: replay's ATM never moved in 30 s. `docs/shots/` re-baselined |
| P19 | Candles in the UI + Chart Style | The main (underlying) chart strip as green/red OHLC candles from `/v2/charts/intraday` (1m / 5m / 15m, latest session drawn, 5-day window feeding the SMAs), with the forming candle updated by feed ticks. A **Chart Style** dialog modelled on the user's reference image: Candle / Line, 7 backgrounds, 6 up and 6 down colours plus a picker, three SMA slots, live preview, Save / Reset. Spec `docs/spec/underlying-candles-v1.md`, 24 rows + amendments 25–34 | 8 code | 75 / 375 / 25 candles drawn to 0.5px from the payload; SMA 20 by hand; Save / Esc / Reset; 3:1 contrast guard; mergeTick; live payload equals Dhan; P6 drawings work; paint p95 < 8 ms; earlier suites green; screenshots read | **done (replay + live history)** 2026-09-19. Spec locked with one `go`. `.cache/p19-verify.js` **62/62**, three runs in a row, 60 s soak with zero console errors. Paint p95 5.6 ms at 375 candles + 3 SMAs. **Live:** `.cache/p19-live.ts` compared `UnderlyingCandleService` with a direct Dhan call for NIFTY and RELIANCE at 1 / 5 / 15 m: 6/6 exact, every OHLC value, 18-Sep session. Regressions: P10a 42/42, P10b 30/30, P9 22/22, P2–P9 re-proof 36/37 and P16 35/37. **The three reds fail the same way on the pre-P19 commit `be8dfff`** (weekend: an empty telemetry ring; P16's funnel reads live NSE numbers against a fixed fixture), so they are environmental, not P19. `docs/shots/` re-baselined once (09/10 now switch to Line first; new 14 and 15). **Open:** the live forming-candle merge can only be watched from Mon 21 Sep 09:15; it is tested through the seam until then |
| P13 | Card layout | *Built by another session on branch `p13-card-layout` (worktree `D:/Temp/Dhan-p13`). Its row lives on that branch; it is noted here only so the numbers do not collide again.* | — | — | see `p13-card-layout` |
| P18 | Public URL | The terminal reachable from any device, with a login. The server side is built in P17 (`APP_PASSWORD` gate). What's missing is the transport. **(a) PC + tunnel** (ngrok is installed and logged in on this PC): every feature works, including the NSE scanner that needs a headed Chrome, but only while the PC is on. **(b) An always-on host** (Render / Railway with a persistent disk): a paid plan and the user's own account. **Not Vercel**: renewing a Dhan token invalidates the old one, so exactly one long-lived process must own it, and a serverless function has nowhere to keep it | ~3 | A URL opened on a phone asks for the login, then shows the live chain | **blocked on a user decision**, 2026-09-19. Starting the ngrok tunnel was denied by the session's permission classifier as "External Ingress Tunnel", and a token store on GitHub was denied as "Data Exfiltration". Both are the user's call, not something to route around |
| P17 | Live data + token upkeep + 09:20 notification | The Data API plan is active (₹499/month, until 17 Oct 2026), so P12's gate is gone. The server now renews its own 24 h token (`src/server/token.ts`). The 09:20 scan sends a phone notification (`src/server/notify.ts`: ntfy, and Telegram once the user makes a bot). A Windows task runs that scan on time (`scripts/scan-task.cmd`), because GitHub started the 18-Sep 09:20 cron at 14:00 IST. One GitHub job per runner at a time, because the two late Windows jobs conflicted on `LATEST-github-windows.md`. `/api/health` reports the running commit, and the UI files are served `no-cache`. Proxied requests need a login | 11 | `npm run check` READY; the live chain matches a direct Dhan call leg for leg; the task fires and a notification arrives; a proxied request without the password gets 401 | **done, except the first 09:20 on Mon 21 Sep** 2026-09-19. `check` READY (plan Active). `live:probe` 17 pass / 2 to read / 0 fail. The live NIFTY chain matched a direct `/v2/optionchain` call on **472/472 legs** (OI and LTP). The live UI screenshot shows the LIVE badge, 237 rows and zero console errors. RenewToken measured: **GET** works (the docs' POST answers DH-905), and it **invalidates the old token** (DH-906). Renewal policy 6/6. Task `DhanNseScan0920` ran from Task Scheduler and its ntfy message was read back from the topic. It was first stuck in `Queued` because the task's default is "do not start on battery"; fixed. Auth: local 200, proxied without password 401, wrong 401, right 200, SSE through the gate streams. **11 files, over the ~8 guideline** — the user asked for all of it in one message |
| P16 | Total redesign (v2) | Two workspaces (Scanner, Option chain) under a 48px nav; Geist + Geist Mono; violet accent on near-black; ATM row as the single focal point; OI butterfly bars; scanner as a full screen with funnel tiles, side-by-side Long/Short and the per-stock trace. Spec `docs/spec/redesign-v2.md`, 18 rows + amendments 19–22 | 5 code | Stage A mock approved; ≥ 19 / ≥ 24 chain rows; scanner without scroll; 2 fonts, 8 sizes; contrast ≥ 4.5:1; every earlier suite green | **done (replay + NSE fixture)** 2026-09-17 — spec locked with `go`, then the Stage A mock approved with a second `go`. Stage B **37/37** at 1440x900 and 1024x800, both themes: 19 chain rows with the chart (23 collapsed in replay, 24 live), a 64px instrument bar even on GOLD, exactly Geist + Geist Mono with every size in the scale, the scanner fitting without scroll, 60 s soaks with zero console errors. **Every earlier suite re-run green**: P10a 42/42, P10b 30/30, P2–P9 37/37, P8 22/22 + 10/10, P14 35/35, server 31/31. Eight amendment rows (19–26), three of them defects the checks missed and the screenshots showed. `docs/shots/` re-baselined once — the first attempt was wrong and thrown away because `shots.ts` did not know about workspaces |
| P15 | Daily 9:20 scan + logs + portable run | `npm run scan:nse` (one scan, logs as JSON + Markdown + CSV, per-stock trace of why each ranked stock passed or failed, top 20/25/30 from one fetch); GitHub Actions `nse-scan.yml` on Ubuntu **and** Windows runners at 09:20 and 09:25 IST Mon–Fri, logs committed to the `scan-logs` branch; README steps for running on another computer from a ZIP; optional `scripts/schedule-windows.ps1` for this PC | 6 | The workflow runs from `main`, reaches NSE, and commits a log; a GitHub ZIP of the branch runs the CLI and the app on a clean folder | **done, except the first scheduled morning** 2026-09-17 — PR #2 merged to `main` with the user's explicit approval (fast-forward of P6–P15). Test runs 35249540540 (branch push) and 35250220510 (`workflow_dispatch` on `main`): both runners reached NSE and pushed logs, 8.9–15.8 s. ZIP of the branch in an empty folder: `npm ci`, `npm run scan:nse`, and the app on port 8790 with a real browser scan all worked. **The Windows task on this PC is NOT installed** — running the script with `-ExecutionPolicy Bypass` was denied in this session. **Open: the first scheduled run, Fri 18 Sep 09:20 IST** |
| P28 | LTP Calculator source analysis | The user's ~133-video transcript corpus for a **third workspace** (LTP Calculator, alongside Scanner and Option Chain), read in full and reconstructed. **127/127 files in `LTP-CALCULATOR/KEY-POINTS/` plus 35/35 in `LTP-CALCULATOR/KEY-POINTS-V2/`** - the latter is **not new videos**, it is the same 35 videos re-extracted from **English translations** instead of Hindi ASR. Output is `LTP-CALCULATOR/ANALYSIS/`: per-file notes for all 127, one consolidated logic document, a glossary + traceability matrix, 39 numbered open questions, and a V2 delta | 9 docs, 0 code | Every source file read end to end, every rule traced to its video number, every contradiction recorded rather than resolved silently | **done (analysis only)** 2026-09-22. **The build is BLOCKED on OQ-1:** the entire product rests on one number, the **reversal price** per strike per side - extensions, divergences, the four 9:20 lines, the eight AI lines, Max Pain, Max Gain, the weekly/monthly bands and the LTP Swing HOI reversals are all that same number selected from a different strike. ~20 files say it is "derived from the Option Greeks"; **none gives the formula.** V2 resolved OQ-24 (the free tier's *data feed* is delayed, the charts are live), OQ-25 (Nifty's weekly expiry **moved Thursday to Tuesday**, which explains three "contradictory" range-generation days), OQ-28 (stock lines carry **per-line** Max Pain/Max Gain) and OQ-31 (LTP Swing bearish wants a **breakout**). It made **OQ-35 worse**: V75 calls a support shift from 25,000 to 24,900 a "bottom-to-top shift", which is only coherent if top/bottom labels are **screen**-relative on a smallest-strike-at-top display - and if so, every WTT/WTB label and the five-state table may be screen-relative too. **No code written; `src/`, `public/`, `scripts/`, `test/` untouched.** `LTP-CALCULATOR/` is untracked and deliberately uncommitted |
| P29 | LTP Calculator spec + the chart criteria made measurable | **(a)** `docs/spec/ltp-calculator-v1.md` — 26 rows, 14 acceptance criteria, locking L0–L3 and L6 (imaginary line, ATM by highest time value, S/R by the outward scan, the 75% grading, the reversal-price ladder). L4–L5 and L7–L10 need accumulated intraday history and are boarded separately. **(b)** OQ-1 attacked by measurement: `npm run oq1:test` / `npm run oq1:live`. **(c)** The chart criteria that had only ever been taken on MCX, added to `open:checks` and to `.cache/p29-chart-live.js`, with their arithmetic proven before the session in `npm run chart:selftest` | 9 | The spec is locked with one `go`; `oq1:test` 5/5; `chart:selftest` 25/25; `open:checks` and the browser suite green on an open NSE session | **spec LOCKED and BUILT** 2026-09-23. **OQ-1 answered `GO` by the user; OQ-35 CLOSED; OQ-33 deferred to L10 with a recommendation.** Engine `src/server/ltp.ts` + `/api/ltp` + the third workspace `public/ltp.js`. `ltp:test` **29/29** · browser `.cache/p29-ltp-ui.js` **17/17** · live recompute **10/10** · `oq1:test` **5/5** · `oq1:live` **3/3** · `chart:selftest` **25/25** · `.cache/p29-chart-live.js` **6 pass / 0 fail / 4 skip** pre-session. The four skips and `open:checks`'s eight need 09:15–15:30 |
| P30 | Session state that is current, and the open-session run re-armed | `/api/health` and `/api/instruments` handed out the registry's `session`, computed once at boot. On 23 Sep a server started 06:10 said "pre-open" all day, so `.cache/p29-at-open.js` never ran and the chip rail's open dot never lit. `withLiveSession()` re-derives it per request. `.cache/p30-at-open.js` waits for a **date** as well as a time, keeps re-checking a replay or stopped server until 15:00 instead of giving up, and names the stale-session case when it sees it | 3 code + 1 script | With the server started before 09:15 and the clock injected to 10:00, every chip's `session` equals `sessionState(now)`; one real session's `open:checks` run lands in the log | **fix done 2026-09-23; the live half waits for Thu 24 Sep.** `npm run session:test` **11/11**, including the frozen registry giving the wrong answer at 10:00 (the 23-Sep bug reproduced). Route-level: a new-build replay server on 8791 read GOLD `open (19:13 IST)` and then `open (19:14 IST)` 65 s later, while the old build on 8787 stayed at `open (19:12 IST)`, its boot minute. `tsc` is clean. **Armed:** PID 24376 for 2026-09-24 → `.cache/p30-open-run.log`. **The 8787 listener at 19:12 was a REPLAY server that this session did not start**, so the run will log `server is replay` until a live server built at P30 or later is on 8787 |
| P31 | Close the open-session criteria from Thursday's run | Read `.cache/p30-open-run.log` from Thu 24 Sep and close P8 AC5, P12b (P8 live), P19/P9 on NSE, P21 on NSE, the NSE `last_price` lag, and P9's opening-candle count | docs only unless a measured red needs a fix | Every open-session criterion has a measured PASS or FAIL from a real 09:15-15:30 NSE session; no SKIP scored as a pass | **Read on 2026-09-24. 19 pass / 2 fail / 3 still open.** That counts the re-measured wheel ratio as a pass and tonight's P19 spacing red as a fail. The runner fired at 09:36:20 and finished at 09:40:04. **PASS:** P21 ×3 (previous close = `ohlc.close`, change = spot − close, = Dhan's `net_change`). The NSE chain `last_price` equals its own quote (Δ 0.00), so the MCX staleness does not happen on NSE. P12b: RELIANCE `net_change` went non-zero at 09:36:39. P19: 1-minute candles are exactly 60,000 ms apart. P19 on NSE: today's session is drawn, 5-minute gaps are 300,000 ms, and the forming candle moves with and contains every tick (601 ticks). P25 on NSE: the anchor held, the chart stayed pinned to the live edge through a new candle, the arrow shows and hides, and a pan keeps its span. OQ-1 live 3/3. Zero console errors. **The P25 wheel-ratio red was the check's fault, not the chart's.** At 09:37 the default view held only the session's first 5 candles, which is chart-nav row 4's floor, so the zoom was correctly refused (ratio 1.000). The check now steps out first. Re-run tonight on NSE: **0.850**. **FAIL: P8 live.** The Dhan scan returned `scored 0` in 0.2 s, and the check never printed why (it does now). Cause, measured tonight: two `/v2/marketfeed/quote` calls sent together on different gate keys (`race:a`/`race:b`) → one got **`805 Too many requests`**. The scanner (`scan:quote`) and the poller's spot quote (`spot:quote`) are exactly such a pair, and the check had just subscribed a chain. `waitForSlot` also does not serialise **in-flight** calls on one key. It only spaces them from the last *completion*. See P36. **Also FAIL (found tonight):** P19's in-session spacing on NSE. Dhan's own `/v2/charts/intraday` for NIFTY `IDX_I` returns a flat **17:55** candle today (direct call, `.cache/p31-nifty-raw.ts`), so the chart has a 2.5 h gap after 15:25. **Still open:** P8 AC5 (it could not run, because the scan did not), P19's 1-minute tick-containment (no whole minute fell inside the 60 s window, re-run), and P9's cross-day `median20` (measured: 11 of 11 of 24-Sep's ATM signals fell in 09:15–09:34; the user decides). *Setup, 2026-09-23:* The replay server on 8787 was killed by PID. A live P30 server (PID 16036) was started, and it renewed the token to Thu 19:25 IST (a replay server never renews, so the token would have died at 05:43 before the 09:36 run). Runner PID 24376 armed. **Risk:** the laptop sleeps after 3 min idle on AC |
| P32 | Auto paper-trading from the front page | **User's direction, 2026-09-23:** an auto trading system that **paper trades**, operated from the UI. Spec `docs/spec/paper-trading-v1.md`, 18 rows + amendments 19–23, locked with one `go`. When armed, the server runs the P14 NSE scan at **09:20 IST**, BUYs the Long list's and SELLs the Short list's **near-month futures** (1 lot, max 10, entries until 09:30), fills at the first feed tick, exits at **stop 1% / target 2%** or **15:15**. A fourth workspace, **Paper**, shows arm toggle, open / closed / 5-day tables, gross day P&L. **No Dhan order endpoint is ever called** | 5 code + 1 test | AC1–AC11 of the spec | **done (replay); AC10 live waits for the next trading day, armed.** `npm run paper:test` **70/70** (AC1–AC5, clock injected, every rule shown rejecting). Browser `.cache/p32-verify.js` **18/18 + 19/19 + 6/6** on a replay server (8791): Run now → the fixture's 1 BUY + 3 SELL; every open and closed P&L recomputed in integer paise equals state **and** screen; kill-by-PID + restart brings the same 4 positions back and the next ticks move them (AC7); only `paper-ledger.replay.json` is written, no live file (AC6); `/paper.js` 200 from a fresh server (AC9); scan-failed state shown in words. Five states screenshotted and read — **three defects were visible only in the PNGs**: two tabs marked selected, the header's buttons wrapping under a long status line, and the 5-day date truncated. **Found on the way, fixed:** neither the LTP Calculator nor Paper ever survived a reload — `scan.js` mapped every non-`chain` `ws` to `scanner`, and `ltp.js` saved `ws` before announcing, so the scanner's `close()` overwrote it. Replay's Run now maps the press to 09:20:00 (amendment 19) or 15:15 would square everything off at once at 21:00. **AC11, 21 suites one at a time on 8791:** tsc clean, paper 70, ltp 29, session 11, oq1 5, chart 25, p25 44, p19 62, p20 30, p22 23, p23 19, p24 0 errors, p10a 42, P2–P9 37, p9 22, p26 27, p25-note 4, p25-pop 6, p29-ltp-ui 17 — all green; **p10b 29/1, and the red is a P29 defect, not P32:** `L` is bound twice (`app.js:729` drawer toggle, `ltp.js:290` LTP workspace — a key no spec row gives it), so p10b's drawer drag lands on the LTP overlay. p10b was never re-run after P29. **Needs the user's call on which feature keeps `L`**; the check was not loosened |
| P33 | Opening-range breakout + 9 SMA exit (F&O stocks) | **User's two voice notes, 2026-09-23.** Replaces P32's entry and exit. The 09:20 scan still picks the stocks; each future's range is the high/low of its 09:15 and 09:20 five-minute candles (`/v2/charts/intraday`, `FUTSTK`, one gate key `paper:candles`); a Long-list stock enters on the first tick strictly above the high, a Short-list stock strictly below the low, 09:25–15:00; each break also buys the near-month stock option nearest the future's price (CE long / PE short); both legs exit on the first tick after **two consecutive completed 5-min closes against SMA9**, or at 15:15. Spec `docs/spec/orb-strategy-v1.md`, 13 rows + amendments 14–19, locked with one `go` | 6 code + 1 test | AC1–AC8 of the spec | **done (replay + one live candle read); AC6 live waits for Thu 24 Sep, ARMED on 8787.** `npm run paper:test` **104/104** (clock injected; a tick AT the level, a Long stock breaking its LOW, one close against SMA9, below/above/below, the forming candle, a failed read retried at 60 s not 59 — each shown rejecting). Browser `.cache/p33-verify.js` **26/26** with a real-time 10-min soak on a fixture replay server (8791): MFSL breaks up and holds, POLICYBZR and FEDERALBNK break down, FEDERALBNK and its PE exit on 2 closes above SMA9 at replay 09:35:05, PNBHOUSING never breaks; every P&L recomputed in integer paise equals state, screen and file; then **14/14** on a fresh run. **Live, read-only:** MFSL future 23 Sep, 77 five-minute candles evenly spaced; range and SMA9 equal a hand recomputation (`.cache/p33-live-candles.ts`). tsc clean; ltp 29, session 11, chart 25, oq1 5. **Browser regression sweep (14 suites, one at a time, 8791): no red traces to P33.** Green: p25 44, p19 62, p20 30, p25-note 4. p10b 29/1, the known `L` red. Every other red is **one environmental cause**: `fonts.googleapis.com` was unreachable (curl 000, `ERR_CONNECTION_TIMED_OUT` on the Inter stylesheet), which fails every "zero console errors" check. That covers p22 22/1, p23 18/1, p24 errors 2, p10a 35/7, p26 26/1, p25-pop 5/1 and p29-ltp-ui 16/1, so every layout check in them measured the **fallback** font. The P2–P9 re-proof first read P5 at **88** subscriptions against 83. The extra 5 were Paper's own open legs on that server (by design, P32 row 12); squared off and re-run, it reads **83 = 2×41+1, PASS**. p9 AC7 frame time was 21/1, with a p95 median of 8.10 ms over 5 runs while another Chrome was busy (CLAUDE.md: not a measurement under load). **Owed:** re-run the console-error checks once Google Fonts is reachable, and p9 AC7 on a quiet machine. **Found on screen only:** option-leg rows drew heavy dashed borders (fixed). **Nearest strike checked against the master** (`.cache/p33-strikes.ts`): FEDERALBNK 332.55 → 335 and 332.49 → 330 in two runs, both right. **Open for the user:** Dhan's series includes the 15:30/15:35 post-close candles, which the morning SMA9 reaches into — `exclude` drops them |
| P34 | LTP Calculator trades on NIFTY options | **User's answers, 2026-09-23:** NIFTY only; buy the strike nearest spot at entry (or the S/R strike itself where the LTP Calculator's rules say so); trading **before** the pressure/scenario filter (L4/L5/L8/L9) exists is accepted, every trade labelled `no scenario filter`. Proposal accepted with `go`: buy CE at the **first touch of the extension of support**, PE at the **first touch of the extension of resistance**; stop at extension **+2** (V114: "extension +2 IS the Max Pain line"); target the **next divergence**; each line used once a day (V53, V100). Built on P29's `readChain()` + `reversalPrice()`, same `PaperTrader` ledger | ~5 | A spec-locked table (touch definition, which snapshot's level, entry window, lots, square-off) and its ACs | **boarded, not started.** Needs its own `spec-lock` for the rows above that are still undefined |
| P35 | Real-money switch (account swap) | **User's direction, 2026-09-23:** paper first; *"if the paper trading results are too good then we will switch it to real money — let's try it once."* The trader gets an `Account` seam with the paper fill engine as one implementation and a Dhan order adapter as the other, selected by one persisted setting. **Nothing places a real order until the user asks for this phase by name** — it needs a results review of P33/P34's paper ledger first, a per-day loss cap, a kill switch, and Dhan's order API contract verified call by call | ~6 | Paper and real produce the same ledger rows for the same signals; the switch is one setting; a real order is never sent without it | **boarded, gated on the user and on paper results.** P32 row 1 (no order endpoint in `src/`) stays true until then |
| P36 | Paper trader survives a sleeping laptop; one marketfeed gate | **Found by P31 on 2026-09-24.** (a) The laptop slept 10:40:04 → 17:45:35. On wake, `onClock` squared both trades off at `p.ltp`, a tick 7 h old. It stamped them 17:45, `reason: eod`, `stale: false`. The ledger reads ₹1,12,985 gross. Priced at Dhan's candles (each exit minute's open), it is ₹2,28,672.50: POLICYBZR's future and PE held to 15:15, while MFSL should have exited at 10:55 on two closes above SMA9. (b) Two `/v2/marketfeed/quote` calls in the same second → `805`, whatever their gate keys. The scanner's and the spot quote's keys differ, and `waitForSlot` never serialises in-flight calls. (c) Dhan's NIFTY `IDX_I` intraday carries a 17:55 candle. (d) The PC clock is 4.6 s behind the exchange. `FETCH_LAG_MS` is 5 s, so a candle read lands 0.4 s after the boundary. | ~5 | A spec-locked table (how a late exit is priced; whether the server keeps the PC awake while positions are open; one gate for the endpoint; the out-of-session candle rule; the clock) and its ACs | **built 2026-09-24, verified on replayed real candles; the live half waits for a trading day.** Spec `docs/spec/sleep-proof-v1.md`, locked with one `go`, rows 1–6 + amendments 7–12. `npm run sleep:test` **37/37**. **AC1:** 24 Sep through the trader, on the real Dhan candles P31 captured (`test/fixtures/paper-2026-09-24/`), by both routes, a restart at 17:45 and a 10:40→17:45 gap in the step clock. MFSL and its PE close at 10:55 (1395.2 / 34.05), POLICYBZR and its PE at 15:15 (1243.2 / 360), all `repriced`. Total **2,28,672.50**, equal to a second implementation from the raw arrays and to P31. **AC2:** a failed read keeps all four legs open and awaiting, a tick does not close them, there is no retry at 59 s and there is one at 60 s, and the date change closes them `stale` with the note. **AC3:** a 59 s gap and a 10:40→10:50 gap (MFSL has one close against) change nothing. **AC4:** 8 window/condition checks, and the shell asks for the hold at 10:40 and releases it after the gap. Live: `powercfg /requests` needs admin, so the flag was proven directly instead: `SetThreadExecutionState` returned `80000000` and then `80000001`, which shows the hold was taken. The child exits within 40 s of its server being killed by PID. **AC5:** two calls on one key issued together → the second left **1044 ms** after the first completed. **AC6 live:** NIFTY 5m today = **75** candles, 09:15..15:25, no 17:55. The P19 spacing check on NSE is back to PASS. `paper:test` 104/104 (its clock now walks in sub-minute steps, amendment 10), tsc clean, ltp 29, session 11, chart 25, oq1 5. Two Paper screenshots (1440 dark, 1024 light) read: the first labels were clipped and were shortened. **Found, predates P36:** the Open table clips Entry/LTP/SMA9 by 1 px at 1024. **Live server on 8787 restarted on P36** (PID 10348, one listener, no EADDRINUSE, token to Fri 18:00 IST), still ARMED. **Browser sweep (8793, one suite at a time): no regression traces to P36.** Every P33 font-outage red is green now, p9 AC7 p95 4.40 ms, and only the known p10b red remains. `p32-verify.js` still asserts P32's superseded rules and needs rewriting |
| P37 | Backtest + nightly learning | **User's direction, 2026-09-24:** replay past days as if live, learn from them regularly, and apply the learning to live. Spec `docs/spec/backtest-v1.md`, locked with `go`. Rows 4/5 were amended and 18 added after a measurement, approved with a second `go`. History store `src/server/history.ts` (real Dhan data, refused in replay), pure engine `src/server/backtest.ts` (its own implementation of P33, parameterised), nightly job at 16:00 IST inside the live server, `npm run backtest` | 5 code + 1 test | AC1–AC7 of the spec | **done 2026-09-24, first run read.** `backtest:test` **31/31**: the engine agrees with `paper.ts` on range, SMA and exit bar (AC2). 24 Sep on the real candles gives MFSL at 10:55 and POLICYBZR at 15:15 (AC3). The due-rule passes (AC7), and replay is refused. **Live:** the first run took **638 calls** over 21 sessions (26 Aug–24 Sep, 14 Sep holiday), 0 failed, 25 MB. The second run took **0 calls** in 29 s with identical trades (AC6). **What it says:** the real-scan days are 21 Sep (0 signals) and 24 Sep (+2,65,917.50, but POLICYBZR's model entry is 1700.45 where live got 1606 through the band jump, row 12). The proxy days total 19 sessions, 2 trades, −1,001.25. **The proxy is structurally wrong on OI:** on 24 Sep NSE had OI rising on the whole losing list (+0.2 to +8.7%) while the near-month future fell on almost all of them (−1.4 to −30.5%, 5 days before expiry, rollover). So the proxy's OI filter almost never passes, and its 2 trades are not a sample of the strategy. Price change agrees within ~0.1–0.7 pp. Walk-forward over 4 trades means nothing yet, and the report says so by printing n. **The learning is real-scan-limited:** row 18 now saves every live 09:20 scan, adding one real day per trading day. Live server restarted on P37 (`aeabd99`), still ARMED |
| P38 | Backtest panel on the Paper tab | Row 15 of backtest-v1: per-day table, current vs suggested settings, real/proxy split, the calibration table | ~3 | spec-lock first | **boarded** |
| P39 | History older than the window | Dhan's expired-contract data, so days before 26 Aug (and September's days once it expires, for strikes never fetched) can be replayed | ? | spec-lock first; one probe call per endpoint | **boarded** |
| P40 | Trades on the phone | Spec `docs/spec/phone-sandbox-v1.md` (P40–P42 locked with one `go`, 2026-09-25). Each live paper entry and exit, a 09:30 digest and a 15:20 summary are pushed through ntfy (`notify.ts`; Telegram too once a bot exists). Live only | 3 code + 1 test | AC1 | **done 2026-09-25.** `phone:test` 18/18: an entry and an exit are one message each, a restart re-announces nothing, and 09:30/15:20 go out once (marked in the ledger). **AC1 live:** a message through `notify.ts` was read back from the ntfy topic **1.9 s** after sending. The research behind the channel choice is at the top of the spec: FrontPage has no API, the Dhan and Upstox sandboxes do not show in their apps, and Tradetron's free plan is the only inbound option, with catches |
| P41 | Tick recorder + no-look-ahead sandbox | Recorder: weekdays 09:14–15:31, all near-month stock futures, plus the 5 CE and 5 PE nearest each future's 09:20 price, plus any leg the live trader holds → `.cache/ticks/<date>/`. Sandbox: a past day replayed through the **same `PaperTrader`**, the clock stepped one tick or 1 s at a time, 5-minute candles built only from delivered ticks, the 09:20 scan withheld until its own timestamp. Synthetic ticks from 1-minute candles for unrecorded days. A Sandbox card on the Paper tab (day, speed 1/10/60/max, rule switches, start/pause/stop), its own ledger under `.cache/sandbox/` | 6 code + 1 test | AC2–AC4 | **built 2026-09-25; the recorder's first real day is today.** `sandbox:test` 17/17. **Row 6 proof:** 24 Sep cut at 11:00 made the same 7 decisions before 11:00 as the full day, and two full runs are identical. The replay reproduces the band jump: POLICYBZR enters at **1606**, as it did live, where P37's candle model said 1700.45. The UI was driven on an isolated live-data server (8793, own CACHE_DIR, pushes off): 24 Sep at max speed gave +1,29,585 as live rules and +3,900 with frozen-range + stop + target. Screenshots at 1440 dark and 1024 light were read: zero console errors, no clipped cells after two fixes. **Found in the first run:** the sandbox's waiting table had Cancel buttons that would have posted a sandbox id to the LIVE exit route (fixed, read-only). The recorder's 5 strikes around the 09:20 price miss a band-jump strike (POLICYBZR bought the 1600 PE, 1660 was the nearest recorded), so it now also records every leg the live trader holds. **AC2 PASS on counts, 2026-09-25, but the day is only partly recorded.** 19,335,431 ticks from all 2,310 instruments (210 FUT, 1,050 CE, 1,050 PE, 0 silent). Counted independently from the CSV by `.cache/p41-ac2.ts`: 2310/2310 instruments equal to `summary.json`, 0 malformed, 0 out of order, and the per-instrument table is `.cache/p41-ac2-2026-09-25.tsv`. **Coverage is 10:45:54 → 15:12:33, with a hole from 12:56 to 13:14:**
- The laptop rebooted at 08:47 and slept 08:54–10:17, so the server only came back at 10:45.
- The trader was armed at 10:46, too late to trade, so nothing held the PC awake. It slept from 12:50 to 13:14.
- It went into Modern Standby again at 14:08, even under a 0x80000001 hold, and stopped at 15:12. That is P46.

There was no 09:20 scan (the Windows task did not run), and no live trade. A sandbox replay of the day loads (from the gzipped file, 59 s), and its proxy scan gives 0 signals, so there is nothing to compare with a live ledger. The day was packed with `ticks:pack` from 1,118 MB to 190 MB, SHA-256 verified. **Open:** the sandbox treats a partial recording as a whole day. A day that starts after 09:25 has no range, and says so nowhere |
| P42 | Risk rules (switchable) | Stop = the other side of the range, target = 2R, skip a range narrower than 0.3%, flag NSE-vs-futures OI disagreement, no entries once the day is at −₹50,000. Each rule can be switched **per sandbox run**. **All off live** until the user says so | in P41 | AC5 | **built 2026-09-25.** Each rule is shown taking one trade and rejecting another (`sandbox:test`), and with every rule off a crash does not close a position (the P33 behaviour). On 24 Sep, frozen-range skips POLICYBZR, the day's winner, which shows why the switches stay the user's call |
| P43 | Combining the calculators | How the LTP Calculator (P29/P34) should combine with the 09:20 ORB scan | ? | the user's definition, then spec-lock | **boarded, waiting on the user** |
| P44 | Train the rules on every F&O stock, 1 Jul – today | **User's direction, 2026-09-25:** "bring this week, last week and full August … train our system … assume all the answers, do not ask anything." Spec `docs/spec/train-all-v1.md`, 17 rows, **locked by delegation** (every unmeasured value is a marked GUESS). The share's 1-minute candles from 1 Jul for all 210 stocks, since the August futures have expired. Selection by 09:20 price only, because NSE's OI figure was measured again today and cannot be rebuilt from Dhan. 1,728 settings, net of costs, walk-forward held-out, and a recommendation only if held-out beats the baseline and both halves are positive. **Nothing changes live.** | 5 code + 1 test | AC1–AC5 | **done 2026-09-25, 244 sessions (1 Oct 2025 – 25 Sep 2026).** Dhan's cash 1-minute history reaches back past the 90-day request limit in chunks: 1,050 calls, 0 failed. `train:test` **43/43**, including AC2, where the trainer equals P37's `replayDay` on 24 Sep's real futures candles and builds 5-minute bars identical to Dhan's. AC3: the share against the future on 99 trades gives the same exit reason in 97, and a mean return difference of +0.10 pp. AC5: 6 trades recomputed to the paisa. **Result:** the live rules minus OI net +5.24 lakh with the model’s fills, but only **+1.63 lakh (t 0.47) when every fill is a minute late**, and they are negative without their best 3 days. Tuning beats the baseline in **all four** walk-forwards (expanding and rolling 60, each with both fill models: +9.33 vs +5.75 lakh, +4.82 vs +2.11 lakh, and so on). **Recommended, NOT applied:** `chg0/either/r3/sma20/x3/SL` (target on or off makes no practical difference), meaning the top 10 movers with no 2% floor, the first break of either side, a 09:15–09:29 range, and an exit on 3 closes against SMA20. It nets +13.07 lakh (t 2.79), or **+7.86 lakh (t 1.70) with late fills**, over 244 sessions, and is positive in 10 of 12 months. Caveats: today's universe for all past days (survivorship), today's lots, no option leg. Details in the spec's Result section. |
| P45 | Shadow-trade P44's recommended setting | Run `chg0/either/r3/sma20/x3/SL/T2R` as a second, labelled paper book next to the live rules. It uses the same feed, and both books go on the Paper tab and the phone digest. That gives a forward test on unseen days before the user decides. It also needs the option leg modelled in P44's report. | ~5 | spec-lock first | **boarded**, needs the user's word to start |
| P47 | NIFTY chain recorder | **Rescoped by the user: NIFTY only.** Spec `docs/spec/chain-recorder-v1.md`. NIFTY's chain for the current and the next expiry, every 3 s snapshot the existing `ChainPoller` emits, 09:14–15:31, ±20 strikes around the ATM, to `.cache/chains/<date>/NIFTY-<expiry>.jsonl`. `ticks:pack` packs closed chain days | 6 | AC1–AC5 | **built 2026-09-25; the live day (AC5) is Mon 28 Sep.** `chainrec:test` **18/18**. On two real snapshots (`.cache/p47-live-shape.ts`), 41 of 269 and 41 of 231 rows were kept with **0 mismatched rows**, about 4 KB a snapshot, so about 30 MB raw per expiry per day. The pack step was checked on a scratch CACHE_DIR, and it left an open day alone. Regression: train 43, sandbox 17, paper 104, ltp 29, sleep 37, phone 18, backtest 31, session 11, all green; tsc clean. **Live 8787 was restarted on `5e8b519` (PID 19840), started detached so it outlives the session.** The previous one died when the last session's process ended. Still ARMED, `check` READY |
| P56 | NSE as the OI referee | **Measured 2026-09-25 after the close:** against NSE's official F&O bhavcopy, NSE's option chain matched OI on 40/40 near-ATM NIFTY legs and **Dhan on 0/40 (up to 21.8% off)**. Volume and LTP were 40/40 for both. Dhan's chain OI is its own 15:39 candle's and misses the final exchange OI. Scripts: `.cache/nse-vs-dhan-chain.ts`, `nse-vs-dhan-atm.ts`, `nse-vs-dhan-oi.ts`, `nse-bhavcopy-oi.ts`, `bhav-vs-both.ts`. Still open: is Dhan's OI also off **intraday**? That needs NSE's chain and Dhan's read in the same minute, a few times on Mon 28 Sep | 0–2 | the intraday agreement is measured, and a rule is written for which OI the LTP engine reads | **boarded; the EOD half is measured** |
| P55 | Research: LTP Calculator's own documentation | The user asked for this as a separate task: find and read whatever official documentation exists (the in-app "Read More" flowchart, ltpcalculator.com's services page, the published LTP Swing flowchart), and map it onto `ANALYSIS/07-OPEN-QUESTIONS.md`. The web search of 2026-09-25 found no GitHub repo or developer docs, only the product sites, apps and pricing page | 0 code | each open question it touches is marked answered, narrowed or not covered | **boarded** |
| P48 | Historical chain rebuild | **NIFTY only**. Spec `docs/spec/chain-rebuild-v1.md` (locked 2026-09-25). `npm run chainhist` stitches `rollingoption`'s ATM−10…+10 × CE/PE (42 series) back into per-strike minute chains in `.cache/history/chains/NIFTY/` (one `.json.gz` per day, `index.json` with the summaries and expiry labels). **680 `WEEK 1` days, 2024-01-01 → 2026-09-25**, plus `WEEK 2` from 16 Sep 2026, 163 MB. Measured: `expiryCode` starts at 1 (0 → DH-905), ±10 is the ceiling, volume in units. AC2 volume vs NSE bhavcopy **0.0104%** (W1) / **0.0191%** (W2). AC4 found one real calendar error: **Diwali 2025, expiry Mon 20 Oct, not the Muhurat Tue 21 Oct**. Fixed; now 0 errors. `chainhist:test` 50/50 | 7 files | AC3 (vs P47's recording) on Mon 28 Sep after 15:31: `npm run chainhist` then `npm run chainhist:verify -- --recording 2026-09-28` | **built; AC3 open until 28 Sep** |
| P49 | LTP state machine (L4, L5, L8, L9) | Pressure's 5 states, COA 1.0, SOC, the Game of Percentage with the shift-completed flag, and the IV gate, over a chain time series | ~5 | spec-lock first | **boarded** |
| P50 | LTP line sets (L7, L10) | 920 lines, AI LTP's 8 lines, the scenario → line-set table, first touch, vetoes. Folds in P34 | ~4 | spec-lock first | **boarded** |
| P51 | Index option backtest | The 920 strategy and AI LTP on the rebuilt history, with option legs priced from their own candles, both fill models, costs, and a walk-forward; reproduces V117's shape | ~4 | spec-lock first | **boarded** |
| P52 | Open questions settled by data | OQ-33 safe/risky, OQ-9 strike depth, OQ-2 volume vs OI, stop size, the gap filter, time windows, which indices | ~2 | spec-lock first | **boarded** |
| P53 | Index paper trader | Tick-driven first-touch entries, limit at the pre-computed premium, state-change exits, a second paper book | ~6 | spec-lock first | **boarded** |
| P54 | Weekly / monthly range (W) | L1–L3 bands, with hit rates measured on history | ~2 | spec-lock first | **boarded** |
| P46 | Keep-awake covers the tick recorder, and actually works | P36 held the PC awake only while the trader trades. **And its hold did not work on this laptop:** 0x80000001 (`ES_SYSTEM_REQUIRED`) was held from 13:55 on 25 Sep, yet Kernel-Power 506 (Modern Standby) came at 14:08:00, the feed stopped at 15:12:33, and the PC woke at 16:19:53. Modern Standby begins when the display turns off, which is after 180 s on AC and 3,600 s on battery here. Fix: the hold is taken while **either** the trader needs it **or** the recorder is recording, and it now asks for the display too (0x80000003). | 2 | Monday's Kernel-Power 506/507 log shows no standby between 09:14 and 15:31 | **built 2026-09-25, live on 8787 (PID 11204, build `0a412e4`), unmeasured until Mon 28 Sep.** tsc clean. The display flag is Windows' documented way to block the display timeout; it has not yet been watched working here. A lid close still sleeps the laptop |
| P14 | 9:20 scanner on NSE data | The user's 14-09 voice notes (P8's scanner restated, naming "NSE spurt") plus a second recording (manual Run button, top 20 "ya 25 ya 30", cross-verify against a pre-defined F&O list). A new source for the existing panel: an off-screen headed Chrome reads NSE's "Securities in F&O" price feed and OI Spurts; `data/fno-list.txt` (210, validated) gates the universe; top N gainers + N losers → abs(chg) ≥ 2% → OI chg ≥ +7%. P8's Dhan path kept as Source `Dhan` | 7 code | Fixture funnels 210→40→26→4 / 210→50→31→4 / 210→60→36→4 with Long MFSL, Short POLICYBZR, PNBHOUSING, FEDERALBNK, agreed by a second implementation; broken fixtures fail as specified; one real NSE scan reconciles; browser checks in both themes | **done (live NSE, market closed)** 2026-09-17 — spec `docs/spec/scanner-nse-v1.md`, server **31/31**, browser **35/35**, P8 10/10 + 22/22, P2–P9 37/37, P10a 41/41, P10b 30/30. Real NSE scans: 5.4 s and 9.1 s. **Open: a scan pressed at 09:20 IST on a trading day** — whether NSE's feeds are fresh by then is unmeasured |

## Now
**P48 built (2026-09-25, 22:45): NIFTY's past minute chains are rebuilt, 680 sessions from Jan 2024.** Summed volume matches NSE's bhavcopy to 0.02%. The one calendar error (Diwali 2025) was found by the data and fixed. Still open: the match against Monday's P47 recording (AC3). Next: P49 (the LTP state machine) reads these files through `readDay(date)`.

**P47 built (2026-09-25, 19:00): NIFTY's option chain will be recorded every 3 s from Monday, current and next expiry.** The user's decisions: **NIFTY only**; **₹20,000 per index trade**, with lots from the option's price and a minimum of 1 lot (DECISIONS.md). Next: P48, rebuilding NIFTY's past chains from Dhan's `rollingoption` (probed working back to Jan 2024). The live server on 8787 is `5e8b519`, detached, ARMED.

**P44 done (2026-09-25 evening): the rules are trained on a year of every F&O stock, and one change is recommended, not applied.** On 244 sessions (1 Oct 2025 – 25 Sep 2026) the live rules minus the OI filter net +5.24 lakh with the model’s fills, but only +1.63 lakh when every fill lands a minute late. `chg0/either/r3/sma20/x3/SL` beats them in all four walk-forwards and nets +7.86 lakh (t 1.70) with late fills. **The live rules are unchanged**: P45 would shadow-trade the recommendation first. P41 AC2 passed on counts, but 25 Sep was recorded only from 10:45 to 15:12, because the laptop slept. P46 (the keep-awake now covers the recorder and asks for the display) is live on 8787, build `0a412e4`, still ARMED.

**P40–P42 built (2026-09-25, 00:30): trades go to the phone, every live tick is recorded from 09:14 today, and the Paper tab has a Sandbox that replays a past day through the live trader without seeing ahead, with switchable risk rules.** Live server 8787 = `53070dc`. The live trader is **DISARMED** (not by this session). The user must press Arm, or nothing trades today.

**P37 built (2026-09-24 night): the backtest runs, nightly at 16:00 IST, and learns only from real 09:20 scans.** 21 sessions are cached (26 Aug–24 Sep). The proxy scan cannot stand in for NSE's OI filter, which was measured and is recorded on the P37 row, so the learning grows by one real day per trading day. Nothing is applied to live without your word.

**P36 built (2026-09-24 evening), live on 8787 and ARMED for Fri 25 Sep.** A sleeping laptop no longer turns exits into silent 10:40 prices. Blind-time exits are priced from Dhan's 1-minute candles, the server holds the PC awake 09:10–15:35 while it trades, the marketfeed `805` collision is gated, and NIFTY's 17:55 candle is gone. Friday's session is its live test.

**P31 read (2026-09-24 evening): the first live paper-trading day happened, and the ENTRIES are right.** Two SELL signals (POLICYBZR, MFSL) entered on real breaks. The range, break minute, fill, option strike and option fill each agree with Dhan's candles. **The exits are wrong** because the laptop slept from 10:40 to 17:45, and the trader closed at the 10:40 tick without saying so. P36 is boarded for that, plus the `805` marketfeed collision that failed P8 live. The open-session suite scored 19 pass / 2 fail / 3 open (P31 row).

**P33 built (2026-09-23 22:40): the paper trader runs the user's opening-range breakout + 9 SMA exit on F&O stocks, future and option legs.** The live server on 8787 was restarted on P33 (build `74c6340`) and is **ARMED** for Thu 24 Sep: 09:20 scan → 09:25:05 range read → entries on a strict break until 15:00 → exit on 2 closes against SMA9 or 15:15. The live candle read was proven tonight on MFSL. Needs the laptop awake at 09:20. P34 (LTP Calculator trades on NIFTY) and P35 (real-money switch) are boarded.

**P32 built (2026-09-23): paper-trading, verified in replay.** It trades nothing until someone presses **Arm auto-trading** on the Paper tab — default is disarmed. Its live half (AC10: the real 09:20 scan's signals entered 09:20–09:30) needs a live server built from P32 on 8787, armed, awake at 09:20 on a trading day. `.cache/paper-ledger.json` is the live ledger; replay's is `paper-ledger.replay.json`.

**P31 set up (2026-09-23 19:25): 8787 is a LIVE P30 server (PID 16036), the token is good until Thu 19:25 IST, and the runner (PID 24376) is armed for Thu 09:36.** The next session is **P32, auto paper-trading from the front page**, at the user's direction. P31's log read can be closed in any session after Thursday's market.

**P30 done (2026-09-23): `/api/health` reported the BOOT-time session, so P29's armed open-session run never fired on 23 Sep.** Fixed per request (`npm run session:test` 11/11), pushed on `p30-health-session`. Re-armed as PID 24376 for **Thu 24 Sep 09:36** -> `.cache/p30-open-run.log`. It needs a **live** server built from P30 or later on 8787; at 19:12 on 23 Sep, 8787 was a replay server this session did not start.

**P28 done (2026-09-22): the LTP Calculator transcript corpus is fully analysed. No code written.**
All 163 source files read end to end - 127 in `LTP-CALCULATOR/KEY-POINTS/` and 35 in
`LTP-CALCULATOR/KEY-POINTS-V2/`, which is **a cleaner re-cut of 35 videos already in V1, rebuilt from
English translations, not new material.** The reconstruction is in `LTP-CALCULATOR/ANALYSIS/`
(9 documents): start at `05-CONSOLIDATED-LOGIC.md`, then `08-V2-DELTA.md`, which overrides parts of
`07-OPEN-QUESTIONS.md`.

**The build is blocked, and the blocker is specific.** Every level the product draws is one number -
the **reversal price** per strike per side - selected from a different strike. ~20 files say it comes
from the Option Greeks; none gives the formula (OQ-1). Second most urgent is **OQ-35**, the
arrow/top-bottom orientation: get it backwards and every directional read on screen inverts.
Third is **OQ-33**, where the published safe/risky line mapping contradicts the only three-month
backtest in the corpus.

**Do not re-read the transcripts.** ~150k tokens to learn nothing new; every rule in `ANALYSIS/`
cites its video number.

**P25 remains done on `p25-chart-nav` (2026-09-22), verified in replay AND against a live open session.**
Both charts navigate like TradingView: wheel zooms time about the cursor, a trackpad pinch does
the same without the page zooming, drag and horizontal wheel pan, alt+wheel and the price gutter
zoom the scale, double-click or `0` resets, and a `⟩` arrow appears the moment the view leaves its
default. The price scale auto-fits to the candles **inside** the window. Spec:
`docs/spec/chart-nav-v1.md`. **44/44** replay with a 60 s soak, **11/11** live on MCX, 4/4 on the
note, 6/6 inside a P26 pop-out.

**Three defects it found by measuring rather than reading**, all fixed: a SHUT session grew a
candle at the wall clock (one bar 5 h 50 min after the 15:25 close in replay); the option window
rounded its stored right edge, so a slow drag panned nothing; and "at the live edge" measured in
milliseconds let a zoom near the right edge silently un-pin the chart from live data.

**The first one had been corrupting P19 for weeks.** Measured against a pre-P25 worktree on its
own replay server: p20, p22, p23, p10a and the P2–P9 re-proof have **byte-identical check lists**
on both builds, and p19 goes **48/62 → 55/62** — seven checks FAIL→PASS, none the other way, and
they are P19's own AC1, AC3 and AC9. P19 was recorded as 62/62 when it was built; nobody re-ran it
until now. **A suite that is not re-run is not green, it is unmeasured.** *(That 55/62 is the
P25-vs-pre-P25 measurement and is correct for the comparison it sits in. **P27 then took p19 to
62/62** — see its board row. Read the row, not this paragraph, for the current state.)*

**GOLD is closed, and the board's diagnosis of it was wrong.** It never "mixed two contracts":
Dhan's option-chain `last_price` is **hours stale on MCX** while the rest of the same payload is
live (57 of 174 strikes moved across two calls 70 s apart). The spot now comes from a quote of the
underlying securityId and the ATM is measured against it. Live: GOLD 6/6, NIFTY 6/6.

**P19's forming candle is verified live** for the first time since 2026-09-01, on the open MCX
session: 395 ticks in 60 s, the forming candle's close equal to the last underlying tick in 12 of
12 samples, its high/low containing every tick, each new candle opening exactly 60,000 ms later.

**What is still unproven needs the NSE session.** `npm run open:checks` now measures all of it in
one command and prints SKIP, never a pass, for anything it cannot reach. Run tonight on MCX:
4 pass, 0 fail, 3 not measurable — P21's open-session rule passed; P8 live, P8's AC5 and the
`NSE_EQ` `net_change` timing need **09:15–15:30 IST on a trading day**.

**Waiting on the user:** merging the stacked branches (74 commits ahead of `main`, in the order
p6 → … → p20 → p21 → p22 → p23 → p24 → p26 → p25), P18's transport, P9's opening-candle question,
`gh secret set NTFY_TOPIC`, and a Telegram bot.

## P29, 2026-09-23 — two of the three blockers closed by reading, the third reduced to one word

**OQ-35 is CLOSED.** The arrow convention is not a contradiction. §3.2 defines *top of the chain*
as the **highest strike** while the chain prints **smallest strike first**, so V112 ("smaller to
larger strike is bullish") is strike space and V121 ("an upward arrow means bearish") is screen
space on that display — up the screen is towards smaller strikes. V45 says so outright and V13's
"support drawn above" follows. **All four are one fact.** Spec row 5 puts every rule in strike
space and derives the screen direction in one function, so the trap cannot recur. Our own grid
already sorts ascending (`derive.ts:106`) — the same orientation as every video.

**OQ-1 has a tested candidate, and is still open.** `reversal(K,call) = K + callLTP`,
`reversal(K,put) = K − putLTP` — the writer's break-even. The corpus's **17 worked (strike →
reversal) pairs** had never been treated as a validation set. The falsifiable test is put-call
parity, which the candidate never mentions: `F = revCall + revPut − K` recovers a spot the
candidate was never shown, on three separate passages, to **+8 / +12 / −6 points**. On the expired
22-Sep chain every ITM reversal lands on spot (23,328.4–23,329.6 vs 23,329) and every OTM one
collapses onto its own strike — the corpus's *"at expiry all reversal prices converge to intrinsic
value"*, reproduced numerically. That line also eliminates three rivals, including a **literal**
reading of V18: "the spot at which time value peaks" is *exactly the strike* under Black-Scholes,
since `dTV/dS` is `delta` below the strike and `delta − 1` above it.
**It is not shipped.** It sits behind one function so any answer is a one-function change.

**A red line that was mine, not the data's.** The live parity pre-check read a median 80.45 on a
6-day chain and 0.45 on an expired one. Parity is `c − p = F − K` and it was being compared against
**spot** — so it was measuring the basis, which is zero at expiry. Rearranged to an invariant
needing no futures price: `c − p + K` is the chain's own implied forward and every strike must
agree. 13 strikes agree to **7.3 points**, basis **+80.5 (34 bp)**. This *strengthens* OQ-1 — the
corpus residuals are the carry, and two of three have the right sign.

**The chart criteria, which had only ever been taken on MCX.** `open:checks` gains P19 on NSE
(1-minute candles exactly 60,000 ms apart; the forming candle contains every tick in its minute)
and P9's opening-candle question **measured** rather than argued. Their arithmetic moved to
`scripts/lib/chart-checks.ts` and is driven by `npm run chart:selftest` against real captured
payloads — **25 checks, each also shown to FAIL on broken input** — because on a shut market
`open:checks` runs only its SKIP branch and there is one session a day to find a bug in the other.
It caught two off-by-one errors in its own expectations. `.cache/p29-chart-live.js` does the
browser half: **6 pass / 0 fail / 4 skip** pre-session, and three of its first four reds were the
script's own (a 14px Style-button icon matched instead of the chart surface; `invX` fed a page
coordinate instead of a chart-local one; a pan asserted on a fully-fitted view, where row 7
correctly clamps it to a no-op).

**P9's opening-candle premise, measured — and it does not hold.** The board has said since
2026-09-19 that *"every live blue fires in the first 20 minutes, because `median20` at the open
reaches back into yesterday's quiet tail."* Counted on the finished 22-Sep session across 18
contracts around the ATM, both sides: **2 of 36 signals land in 09:15–09:34 — 6%.** The first half
of the premise is real — `median20` at 09:15 sits far below the session's own first-20 median, four-
fold on some strikes (23,450 CE: 23,692 against 98,312) — but it is **not sufficient**, because row
7's OI half must fire as well and OI has barely moved by 09:20. One session, one instrument, one
expiry, so the 09:36 run adds today's. `node .cache/p9-opening-question.js` (`SPAN=4`). Its first
run lost 6 of 10 contracts to `DH-904` and would have concluded from the 4 that survived; it is
paced now.

## Next 3
*(Updated at P48 close, 2026-09-25 22:45.)*
0. **Mon 28 Sep, 10:00–14:00 (P56):** run `node --env-file=.env .cache/nse-vs-dhan-chain.ts` three times, an hour apart, to see whether Dhan's OI matches NSE's **during** the session. (After Friday's close it was 0/40, against NSE's 40/40.)
1. **Mon 28 Sep after 15:31:** close P47 AC5 (`cat .cache/chains/2026-09-28/summary.json`, both expiries, largest gap ≤ 10 s), P46 (Kernel-Power 506/507 between 09:14 and 15:31), P41 on a full tick day (`node .cache/p41-ac2.ts 2026-09-28`), and the live paper day. Then `npm run ticks:pack`.
2. **Mon 28 Sep after 16:00 (P48 AC3):** `npm run chainhist` (it adds 28 Sep, about 84 calls), then `npm run chainhist:verify -- --recording 2026-09-28`. P48 closes on that verdict.
3. **P49** (the LTP state machine), and then the LTP Calculator 7-day premium for ground truth, once P48 and P49 exist.

*(P40-P42's list, kept for the record:)*
1. **Fri 25 Sep after 15:31:** read `.cache/ticks/2026-09-25/summary.json` (P41 AC2), replay today in the Sandbox on recorded ticks and compare with the live ledger, confirm `[backtest] done` after 16:00 and the saved scan in `.cache/history/scans/2026-09-25.json`.
2. **User calls owed:** Arm the live trader (it is disarmed); which P42 rules go live after sandbox runs; how the calculators combine (P43); `w32tm /resync`.
3. **P38** (backtest panel) or **P34** (LTP Calculator on NIFTY) — whichever the user picks next.

1. **Thu 24 Sep after 15:15:** read `.cache/paper-ledger.json`. Recompute every range, entry, SMA9 exit and P&L from Dhan's real 5-minute candles (`.cache/p33-live-candles.ts <SYMBOL>`), and close P33 AC6. Also read `.cache/p30-open-run.log` and close P31's open-session criteria.
2. **P34 spec-lock:** LTP Calculator trades on NIFTY options. The user's answers are on the board row. Still undefined: the touch rule, which snapshot's level, the entry window, lots and the square-off.
3. **User calls owed:** `exclude` or keep the post-15:30 candles in SMA9; add a hard stop (`change 10`); who owns `L`.

*(P32's list, kept for the record:)*
1. **Restart 8787 on the P32 build, then arm Paper.** The live server (PID 16036) was started before P32, so `/paper.js` 404s on it and the Paper tab is dead on the live screen. Restarting it was refused by this session's permission classifier, so the user restarts it: `taskkill //F //PID 16036`, then `npm run dev`. Then open Paper and press **Arm auto-trading** before 09:20 Thu, with the laptop awake. This measures AC10 and keeps the P31 runner's live server.
2. **Decide who owns `L`**: the telemetry drawer (`app.js:729`, terminal-redesign-v1) or the LTP Calculator (`ltp.js:290`, no spec row). p10b is 29/1 until then.
3. **P31:** after Thu 24 Sep's session, `cat .cache/p30-open-run.log` and `.cache/paper-ledger.json`, and close the open-session criteria plus P32's AC10.

*(P31's list, kept for the record:)*
1. **P32: auto paper-trading from the front page.** Run `spec-lock` first: strategy/signal, exit, size, instruments, UI. Paper only, with no Dhan order endpoint and separate replay and live ledgers.
2. **P31:** after Thu 24 Sep's session, `cat .cache/p30-open-run.log` and close the open-session criteria.
3. **Merge the stacked branches in order** (p6 -> ... -> p25 -> p29 -> p30 -> p31).

*(P30's list, kept for the record:)*
1. **Thu 24 Sep, before 09:36:** make sure 8787 is a live `npm run dev` built from P30 or later. Then read `.cache/p30-open-run.log` and close the open-session criteria (P8 AC5, P12b, P19/P9 on NSE, P21, NSE `last_price` lag).
2. **P9's opening-candle decision:** 2 of 36 signals fell in 09:15-09:34 on 22 Sep. Add 24 Sep's count, then decide or close.
3. **Merge the stacked branches in order** (p6 -> ... -> p25 -> p29 -> p30), then spec L4-L10 of the LTP Calculator.

1. **Answer spec row 12 (OQ-1) in one word** — `go` / `theoretical` / `api` / `hold` — and the
   rest of `docs/spec/ltp-calculator-v1.md` with one `go`. Then build L0–L3 + L6.
   *(Superseded item, kept for the record: "Decide OQ-1, OQ-35 and OQ-33 - then, and only then,
   P29: lock the LTP Calculator spec." OQ-35 is closed and OQ-33 is deferred to L10 with a
   recommendation; the spec is written.)*
   OQ-1 is the reversal-price formula: either the app's API exposes it, or it is reconstructed from
   V18's hypothesis (the spot level at which a strike's time value peaks) and validated against the
   dozens of worked strike-to-reversal pairs in the notes, or it ships as a documented pluggable
   approximation. OQ-35 and OQ-39/OQ-30 need a video and the *published* LTP Swing flowchart
   respectively - neither is answerable from this repo. Everything downstream is already written up.
2. **The next trading morning, 09:15 — `npm run dev`, then `npm run open:checks`.** It drives
   `/api/scan?source=dhan` live and recomputes the funnel from the scan's own per-stock trace,
   measures P8's AC5 (no two chain calls closer than 3000 ms while that scan runs), records the
   clock time `NSE_EQ` `net_change` first leaves zero, re-checks P21 on NSE with the session on,
   and answers the one question P25 left open: **whether NSE's option-chain `last_price` lags its
   own quote the way MCX's does.** Also read `.cache/scan-task.log` for the 09:20 task. The
   21 Sep run fired on time (09:20:04, prices as-of 09:19:48 — 16 s stale, `status=ok`).
3. **P9's opening-candle decision, then the PRs.** Ask whether `median20` should stay inside the
   session — every live blue fires in the first 20 minutes because `median20` at the open reaches
   back into yesterday's quiet tail. Then merge the stack in order.

## Session log
| Date | Phase | What happened |
|---|---|---|
| 2026-09-22 | P28 | **The LTP Calculator corpus, read in full and reconstructed - understanding only, at the user's explicit instruction not to build.** 127 of 127 files in `LTP-CALCULATOR/KEY-POINTS/` (~104,000 words) plus, arriving mid-session, 35 of 35 in `LTP-CALCULATOR/KEY-POINTS-V2/` (~25,800 words). **V2 is not new videos:** its own `INDEX.md` says the 35 are videos already in V1, rebuilt from **English translations** of the same transcripts instead of the Hindi ASR captions, while the other 92 were already English and were not redone. Nothing was sampled - several rules exist in exactly one file (V108's five-state pressure table, V109's percentage **inversion** after a shift completes, V110's provenance for each AI line, V118's straddle-to-next-week's-range projection, V126's reversal-price-gap directional rule), so a summary-of-a-summary would have lost them. **The system, reconstructed:** locate support on the put side and resistance on the call side by scanning outward from the imaginary line; grade each by whether its second-highest challenger crosses **75%**; convert that into a directional **pressure** via five states (a level that *attempts* a shift and abandons it pushes the **opposite** way; one that *completes* it pushes the way it went); combine the two pressures into one of **nine scenarios**; the scenario decides which lines are drawn and which side may be traded; each line is a **reversal price**, with **Max Pain = stop loss** and **Max Gain = target** - neither carrying its industry meaning. **Three things that will bite an implementation:** pressure cannot be computed from a snapshot (it needs the level's intraday history - V25 is a whole video about a latecomer reading "scenario 1" when it is scenario 5 with the support already shifted); the percentage **inverts** once a shift completes, and three files give three different completion tests; and the arrow convention is stated two opposite ways. **V2's contribution:** resolved OQ-24, OQ-25, OQ-28, OQ-31; narrowed OQ-13 and OQ-23 (Bajaj Finserv's risk is **4**, not 1 - so taking it at all broke the tool's own "0 or 1" rule, which is very likely the mistake V71's teacher refused to name); made **OQ-35 worse**; added OQ-39. **V2 also introduced one error of its own** - it flags same-strike support and resistance as transcription damage, when V11/V12/V23 teach that case explicitly as the zero-divergence day. **Cross-session:** three peers were live in this checkout; `dhan-36` asked to kill the servers on 8787/8791 and was told neither was mine, with two cautions (check `git worktree list`, kill by PID never `//IM node.exe`). It then **corrected me** - I had endorsed its read that both servers were stale, and `/api/health`'s `build` is HEAD **at process start**, not the code loaded. Both traps are now in `CLAUDE.md`. Nothing committed; `LTP-CALCULATOR/` is untracked and not gitignored, and every peer was asked not to sweep or clean it. |
| 2026-09-22 | P25 | **TradingView-style chart navigation, and three defects it found by measuring** (`docs/spec/chart-nav-v1.md`, 20 rows + amendments 21-28). Wheel = time zoom about the cursor at **0.85/notch** with the time under the pointer held to **0.5px**; trackpad pinch the same with `preventDefault()` so the page never zooms; shift/horizontal wheel and a plot drag = pan; alt+wheel and a wheel over the 76px gutter = price zoom at 0.9; double-click or `0` = reset; a `⟩` arrow appears the moment the view leaves its default. The price scale **auto-fits to the candles inside the window**, so a zoom shows that window's range instead of the session flattened. The option window gets the same gestures in **index** space (P9 draws option candles indexed so a halt cannot stretch a bar). **44/44 replay** with a 60 s soak, **11/11 live** on the open MCX session, 4/4 note, 6/6 in a P26 pop-out; paint p50 1.6 / p95 3.2 ms while panning at 375 candles + 3 SMAs. **Three defects, none of them visible by reading the code:** (1) `ucandles.onTick()` grew a candle on a **SHUT** session - in replay at 21:15 the chart held one bar **5 h 50 min** after the 15:25 close with an empty 21,000,000 ms stretch in front of it, while `/api/ucandles` had returned 76 clean candles, so the client drew the gap; the server's `openNow` decides now. (2) The option window stored its right edge as a **rounded** candle index, so eight drag steps of 0.12 candles each rounded back to where they started and a **slow** drag panned nothing while a fast flick worked. (3) "At the live edge" measured in **milliseconds**: six notches with the cursor 1px short of the right edge drifted 144,450 ms - invisible - and silently un-pinned the chart from live data. It is measured in pixels now. **Regression measured, not assumed:** a pre-P25 worktree on its own replay server (8792) gave byte-identical check lists for p20 19/29, p22 20/22, p23 18/19, p10a 32/42 and the P2-P9 re-proof 36/37, and **no check fails on P25 that passes on the baseline**. p19 went **48/62 -> 55/62**: seven FAIL->PASS, none the other way, and they are P19's own AC1/AC3/AC9 - red since the phantom candle appeared, and nobody had re-run that suite since it was recorded at 62/62. **Also closed, live:** GOLD's header (the board called it "two contracts"; it is a **stale `last_price` on MCX** - the chain returned a 13:20-14:05 print at 21:35 while the same securityId quoted 152,396 and 57 of 174 strikes moved across two calls 70 s apart), so the spot and the ATM now come from a quote of the underlying - GOLD 6/6, NIFTY 6/6; and **P19's forming candle**, watched live for the first time since 2026-09-01. New: `npm run open:checks` for the five NSE-session-only measurements, SKIP never scored as a pass. Branch `p25-chart-nav`, pushed. |
| 2026-09-21 | P26 | **Panel windows and blip recovery** (`docs/spec/panel-windows-v1.md`, 12 rows, locked with one `go`, 7 amendments). The session started as an audit: the user photographed `could not reach the backend — TypeError: Failed to fetch` in the option-chart window and said the candles were not forming. **Neither was a data bug.** `/api/ucandles?key=NIFTY&interval=1` returned **26 consecutive 1-minute candles with zero gaps**, and a browser measurement found all **94** session candles inside the frame (`inside 94, above 0, below 0`). The screenshots were taken during a **40-second window in which the server was being restarted**. Reproducing the outage against the replay server produced the "before" table in the spec: the server was back in 5 s and the chart stayed dead for a **full 60 s**, because the 60 s refresh cadence was being reused as the retry cadence. Now both candle stores climb a **2 → 4 → 8 → 16 → 32 → 60 s** ladder, reset on the first success, show a live countdown and a **Retry now** button, and — row 10 — refetch at once on the SSE's `off → on` edge. Measured recovery: **0.3 s and 1.3 s** on two runs. On top of that, three panels (chart, chain, option window) each got `⛶` fullscreen (the native API, so Esc is the browser's) and `⧉` pop-out, which is **this same page with `?pop=<panel>`** — no new file under `public/`, so no `STATIC` row and no 404 trap — opening its own SSE on the **same poller key**, so Dhan sees no extra calls. **34/34 in replay** (27 + 7), then **27/27 again against the live server with the market open** — the first phase here verified live rather than only in replay. **Three defects found after the first commit, two of them only by reading a screenshot:** one Esc in a fullscreen option chart both left fullscreen **and** closed the contract (amendment 17); the popped-out option chart stayed a 560×340 box in the corner because `candles.js` writes its geometry as **inline styles**, which beat every class rule — and **every assertion in the suite passed** through two wrong versions of it (amendment 18); and the pop-out glyph shipped as `U+29C8` instead of the locked `U+29C9`, caught by a 3× close-up of the button (amendment 19). **Two harness bugs were fixed rather than the claims loosened:** `offsetParent === null` calls a `position:fixed` element hidden when it fills the screen, and handing `page.evaluate` back as a pending promise raced the key press, which read as a flaky product. **What is NOT claimed:** Chrome's own "Esc exits fullscreen" is in the browser UI layer and a CDP key press does not reach it, so only the app's half is measured — Escape is not `preventDefault`-ed and nothing in the app acts on it. One manual press still owed. **Pre-existing and left alone:** `/favicon.ico` 404s because `STATIC` has no favicon row; counted separately instead of folded into a green. **Five code files** plus the spec and this board. Branch `p26-panel-windows`. |
| 2026-09-20 | P24 | "Chart gayab ho gaya." Not a bug in the chart: the pane was **collapsed** (`localStorage.chart = '0'`, set by `C` or the ▾ button, and persisted), and the only way back was a 24px arrow rotated to ▸ at the far right. Collapsed, `#chartBtn` now reads **▸ Show chart** (accent border, title and aria-label to match). `.cache/p24-verify.mjs` on the live page: collapsed load shows the label, one click restores the 200px strip with candles, it survives a reload, `C` collapses again; zero console errors |
| 2026-09-19 | P23 | **Spot window.** The user, with NIFTY at 23,346.40: "lower 23,700 tak, upper 22,950". Rule (`docs/spec/spot-window-v1.md`): 8 strikes strictly below the spot + 8 at or above it = **16 rows**, replacing P20's ATM + 8 each side = 17. Chip `Spot ±8 · 16 of N strikes`. `app.js` only, plus the P19 price-axis label that peeked out behind the last-price pill (`ucandles.js`). `.cache/p23-verify.mjs` recomputes the rows from the snapshot the page itself received: replay 19/19 (all six chips), **live 20/20 — NIFTY shows exactly 22,950 … 23,700**. Superseded 17-row checks rewritten in `.cache/p23suite/`: P20 29/29, P10a 42/42, P2–P9 37/37, P9 22/22, P19 62/62, P10b 30/30, P22 22/22, P16 35/37 (the two live-NSE funnel rows, environmental). `docs/shots/` re-baselined, `01-chain-light` read |
| 2026-09-19 | P22 | **Type, fit and chart height** (`docs/spec/ui-type-v1.md`, 9 rows + amendments 10–13, locked with one `go`). Measured first: 277 of 425 chain cells cut off with Greeks on at 1620 wide. Built: Inter everywhere (HTML + every SVG `MONO` constant, one family, tabular figures); the grid on `table-layout:auto` so cells grow to their text; the P7 peak marker a 6px tick under the digits; chart plot 112 → 200px; the session note on the time-axis row; a labelled **Style** button (icon-only below 1180px — amendment 13, the header wrapped at 1024); the option window opens over the side NOT clicked (P20's open decision). `.cache/p22-verify.mjs` **22/22 replay, 23/23 live** (17 of 17 rows at 1440×900 live, 16 in replay because of the notice line). Regressions, superseded checks rewritten in `.cache/p22suite/` not dropped: P20 29/29, P10a 42/42, P2–P9 37/37, P9 22/22, P19 62/62, P10b 30/30, P16 36/37 (the red is the live-NSE funnel vs its fixed fixture, environmental since P19). **Cost, measured:** Greeks on now scrolls sideways 270px at 1440 and 90px at 1620 (was 44px with the text cut off). `docs/shots/` re-baselined; `01-chain-dark` and `13-scanner-dark` opened and read |
| 2026-09-19 | P21 | **The header's spot change read `+0.00 (+0.00%)` on a shut market; TradingView read `+75.80 (+0.33%)` for the same NIFTY close.** Cause, measured with direct Dhan calls: after the session `/v2/marketfeed/ohlc` returns that day's own close as `close` (NIFTY 23346.4 = `last_price`; `quote` `net_change` 0). Fix in `poller.ts` only: the reference is the `/v2/charts/historical` daily close of the session before the one the intraday payload ends on. Live after the fix, all six chips: NIFTY +75.80 (+0.33%), BANKNIFTY +302.95 (+0.54%), SENSEX −19.63 (−0.03%), RELIANCE −17.50 (−1.41%), HDFCBANK +18.00 (+2.52%) — each equal to TradingView's watchlist. Replay unchanged (NIFTY −83.28). The same comparison showed the candles agree: 12:15 H/L identical to the paisa, O/C within 0.25. **Open:** GOLD's chain spot (153,176) is not its charted future's LTP (154,263), so GOLD's change mixes two contracts, before and after this fix; and the daily candle's behaviour during a live session is unmeasured until Mon 21 Sep |
| 2026-09-19 | P20 | Spec-locked with one `go`. The chain shows ATM ± 8 (17 rows); the option chart opens in a floating window and the NIFTY strip stays. 29/29 in replay; superseded checks in five older suites rewritten to the new rule. The screenshot showed the window covering the ATM row's PE side, which is recorded for the user. Branch `p20-strike-window`. |
| 2026-09-19 | P19 | Spec-locked (24 rows, one `go`) from the user's request plus a reference image of a "Chart Style" dialog. Built `/api/ucandles`, the candle strip and the dialog. The suite found one real bug (Esc stopped closing the dialog after a swatch click: the re-render dropped focus to `<body>`), now fixed and recorded in CLAUDE.md. 62/62 in replay, live history equal to Dhan 6/6. Branch `p19-candles`. |
| 2026-09-19 | P12b | Live drive on a shut market. The feed (quote/full/OI packets), P7's peaks and P9's 488 candles were checked against direct Dhan calls, and all agree. Found and fixed replay/live sharing `peak-oi` / `iv-baseline` / `scan-oi` caches. P8 and AC5 wait for Monday. The user asked for red/green candles in the UI, which became P19. Branch `p12b-live-drive`. |
| 2026-08-19 | — | PRD, API contract, spec lock and UI contract written. Dhan API facts and five of six security ids verified against the live instrument master. Awaiting `go` on `docs/spec/option-chain-v1.md`. |
| 2026-08-19 | P0 | Spec approved (`go`). Backend skeleton built on Node 24 native TypeScript — no build step. Master downloads (35.7 MB, 185,088 tracked rows) and resolves NIFTY 13/lot 65, BANKNIFTY 25/30, SENSEX 51/20, RELIANCE 2885/500, HDFCBANK 1333/650. Session windows verified live: equity chips reported closed at 20:57 IST while MCX reported open. `tsc --noEmit` clean. **GOLD blocked**: SPIKE-01 needs `.env` credentials; candidates 483079 (near-month FUTCOM) and 114 surfaced in `/api/health`. Branch `p0-skeleton`, commit `02f0e4b`, no remote configured yet. |

| 2026-08-19 | P1-P4 | Ran all remaining phases in one session at the user's explicit request. Backend: DeriveEngine, ChainPoller (completion-scheduled 3000 ms), TelemetryBus (500-sample ring), SSE stream with telemetry backfill, CSV export, IV baseline store, previous-close cache. UI: 23-column grid with the strike spine, ATM auto-centring, spot marker, ITM tint, OI bars, change flash, latency panel, expressive states, both themes, keyboard map. **Verified in replay:** PCR and ATM IV recomputed from the payload match exactly; 41 rows x 26 fields with zero NaN/Infinity; cadence min gap 3184 ms across 7 consecutive polls; 14 screenshots captured and reviewed. **Live data still blocked** - the supplied access token is rejected by Dhan (`DH-906 Invalid Token`, `808 Authentication Failed`) on every endpoint including `/v2/profile`, so SPIKE-01 could not run. |

| 2026-08-27 | P5 | Tick-by-tick added on the user's request: Dhan WebSocket live feed (`feed.ts`) with a hand-written binary packet parser, per-contract subscriptions derived from each snapshot's own strike list, 10 Hz tick batching over SSE, and a live line/area chart strip above the chain. Feed carries LTP/volume/OI only, so IV and greeks stay on the 3 s REST path and the UI labels which is which. **Verified in replay:** 83 subscriptions for 41 strikes with zero orphans, 92 underlying ticks in 12 s, frame gaps 99-237 ms, header/chart/spot-marker all agreeing. **Two real bugs found and fixed by testing:** subscriptions were built from the master (462 contracts, 141 never rendered) instead of the snapshot; and the reconnect backoff never escalated because an opened-then-dropped socket reset the failure count, producing a 1-per-second reconnect loop forever. **Live still blocked:** token is valid but `/v2/profile` reports `dataPlan: Deactive` - option chain returns `806 Data APIs not Subscribed` and the feed socket is accepted then dropped with code 1006. The binary parser has therefore never run against real bytes; `npm run feed:probe` exists to check it the moment the plan goes active. |
| 2026-08-28 | P6 preview | Ran the app for the user in replay mode and drove it in a browser. 6/6 instruments resolve - **GOLD now resolves on its own** (`483079 / MCX_COMM / lot 1`), closing the P0 blocker. 41 rows x 23 columns, streaming confirmed across two frames 6 s apart (spot 24,078.67 -> 24,112.47, 49 -> 56 t/s, RTT 220 -> 192 ms), chip rail exercised on GOLD / BANKNIFTY / RELIANCE, zero console errors. No `src/` changes. **Live data still blocked**: token valid to 16:52 today but `dataPlan: Deactive`, option chain returns `806`. Boarded P6 (chart price axis + drawing tools) and P7 (peak-OI tracking) from the user's questions; retro-filled the missing P5 row from the 2026-08-27 log. |
| 2026-08-28 | P8/P9 spec | Six voice recordings analysed into requirements. **Two separate systems**, not one: P8 a 9:20 F&O stock scanner (top gainer/loser -> LTP change >= 2% -> OI change >= 7%) and P9 option-candle colouring (blue/yellow on the option's own chart when a big player enters or exits a strike). Rec 03 and 04 are word-for-word duplicates, so the scanner has 3 filter steps, not 4. **Verified, not assumed:** the NSE F&O universe is exactly **210** stocks (FUTSTK and OPTSTK underlying lists identical after dropping 18 `NSETEST` dummy scrips); all 210 fit in ONE `/v2/marketfeed/quote` call (limit 1000), so scanner steps 1-2 cost ~1 s against a 2-minute budget; `/v2/charts/intraday` accepts `NSE_FNO`+`OPTSTK` and returns per-candle `open_interest`, which makes P9 buildable and **corrects this morning's wrong claim that peak OI must be self-recorded**; `nseindia.com` refuses connection from this machine (HTTP 000 while example.com and dhanhq.co return 200) and its OI Spurts page lists only the top 25, so scraping it is both blocked and lossy. No `src/` changes. Specs deliberately left unlocked - 15 open questions and one contradiction (R01 says volume triggers the colour, R02 says OI). |

| 2026-08-31 | P6 | Chart price axis + drawing tools. Spec-locked first (`docs/spec/chart-tools-v1.md`, 27 rows), then built in a new `public/chart-tools.js`. Drawings are stored as **(time, price) anchors, never pixels**, so they survive a scale drag, a range switch and a reload by construction. Price axis: drag the 76px gutter, `zoom = clamp(startZoom * exp(dy/180), 0.15, 12)` applied to the half-span of the auto-fit range about its midpoint so the chart keeps tracking price; double-click resets to exactly 1. Crosshair snaps to a real tick within 24px. Four tools (trendline, horizontal line, ray, rectangle), one-shot, 6px hit tolerance, draggable end handles, two-click clear-all, capped at 200 per (instrument, expiry), persisted under `draw:v1:<inst>:<expiry>` with stale-expiry keys pruned at boot. **Verified in replay: 19/19 checks pass**, zero console errors — anchors byte-identical across an instrument switch and a reload; endpoints within **0.048px** of `X(a.t)/Y(a.p)` recomputed after a zoom drag to 0.46x; crosshair pill equals `invY(pointerY)` to 2 dp; 5px selects and 8px does not. **Two amendments during the build, both approved:** row 28 (dedupe horizontal-line price pills at 18px, the pill's own height) and AC8 restated from *max* to *p95* — profiling showed an 8ms **max** is unreachable with **zero** drawings (0 shapes: p50 2.40 / max 6.40ms), so the criterion as written could never pass. Two real optimisations came out of that measurement: one `<path>` per style instead of one node per shape, and the pill dedupe. Result at 200 shapes: **p50 1.70 / p95 2.70 / max 7.40ms**, faster than the old zero-shape baseline. 12 screenshots reviewed in both themes. Branch `p6-chart-tools`. |
| 2026-08-31 | P8/P9 spec | **Both specs locked** — `docs/spec/scanner-v1.md` (17 rows) and `docs/spec/option-candles-v1.md` (19 rows), approved with one `go`. All 15 open questions closed. The two that had been called undecidable were answered from the material rather than invented: **"top gainer/loser" = top 50 gainers + top 50 losers of the 210-stock universe itself**, taking the 50 from the user's own worked example ("maan lo 50 stock aaye") and keeping the three filters distinct; and **blue = `dOI > 0` = a big player entering the strike = buy, yellow = `dOI < 0` = exiting = sell**, read off the board's own wording rather than guessed, with the spec naming it the one row to revisit first if the screen ever looks backwards. The volume-vs-OI contradiction (Rec 01 vs Rec 02) is resolved as **AND**, because OI cannot move without volume so `OR` collapses to the OI test alone. Two prerequisites surfaced while writing the specs: `FUTSTK` must be added to `KEEP_INSTRUMENTS` in `src/server/master.ts` or stock futures OI is unreachable, and both new `public/*.js` files need `STATIC` rows or they 404. Seven terms appended to `GLOSSARY.md`, including a numeric definition of "vibration". No `src/` changes; both builds still blocked on the Data API plan. **P10 boarded in the same session at the user's request** — a full TradingView-style rebuild of the screen, the chain as one scrollable table with the complete strike list, and the latency data re-presented in a different form. Deliberately left **unlocked** and explicitly ordered **last**, after the recording-derived phases: it is three undefined adjectives, ~12 files against an 8-file rule, and it would rewrite a screen carrying six phases of passing acceptance criteria. |

| 2026-08-31 | P7 | Peak-OI tracking. Spec-locked first (`docs/spec/peak-oi-v1.md`, 20 rows, approved with one `go`), then built in a new `src/server/peakoi.ts`. The previous **trading** session is derived from the candle data itself — the latest IST date before today that has candles — so there is no holiday calendar and no weekday arithmetic; one call on the underlying settles it per instrument per day, and every contract call reuses it. `peak = max(open_interest)` over **that day's candles only**; today's arrive in the same window and are discarded. All 82 contract calls share **one** rate-gate key (`waitForSlot` is per key — a key per contract would have fired all 82 at once), the results are cached to `.cache/peak-oi.json` under `(sessionDate, securityId)` and never re-fetched, and the backfill runs ATM-outward while a subscriber is attached. **Verified in replay: 23/23 checks** — 7 against the SSE payload, 16 in the browser. Peak equals the hand-computed max to the unit (52,884 = 52,884 over 375 candles); a today-dated candle at **99x** does not move it; exactly **5** breached cells (3 CE, 2 PE) at the seeded offsets; `Pk %` agrees with the OI cell and the tooltip peak on all 82 cells; chain cadence held at **min 3133 ms per key** across the backfill; zero console errors; six screenshots reviewed in both themes. **Two rows added during the build (21, 22)** and **one deviation**, all three found by testing rather than by reading: on a **closed market** the poller emits one snapshot and never again, so the column would have stayed empty for anyone opening the app after 15:30 — the store now notifies as contracts land and the poller re-emits, throttled to 750 ms; and the replay tick feed drew a **fresh uniform random OI (1 L to 41 L) per contract per tick**, so any ratio against it was noise — seeded from the snapshot and random-walked instead, with the chain's flat `900/poll` drift made proportional for the same reason. **Nine code files, one over the ~8 guideline**: the two replay-feed files were not optional, because without them P7's own acceptance criteria could not be measured in the only mode available. **The live path has never run.** Branch `p7-peak-oi`. |

| 2026-09-01 | P8 | The 9:20 F&O scanner. `FUTSTK` added to `KEEP_INSTRUMENTS` first - the prerequisite the spec named - which made stock futures OI reachable for the first time and cost **+1,270 rows on ~170,465**, measured rather than feared. The funnel runs `210 -> 208 scored -> 100 ranked -> 87 -> 6` and returns **4 long + 2 short**. **Verified in replay: 32/33 checks** across three scripts - 10 from a **second, independent implementation** that rebuilds the whole funnel from the same payload with its own arithmetic and its own candle reducer and never imports `scanner.ts` (it agrees on the universe, both filter counts, both candidate lists by name, the skipped set, the rejected count, and that every survivor is inside the top 50 of its own side), 22 in the browser, and 5 static/responsiveness checks. Cold **4.2 s**, warm **43 ms**. Zero console errors; 5 screenshots reviewed in both themes. **Two real bugs found by testing:** the master lists an **NCD** under `CHOLAFIN` and `MOTHERSON` with `INSTRUMENT = EQUITY`, so matching on symbol alone would have quoted a debenture's price as the share's - `MasterRow` grew a `series` field and the cash row must now be `SERIES = 'EQ'`; and `.scan{display:flex}` outranked the UA's `[hidden]{display:none}`, so the panel was on screen from page load and neither Esc nor the close button could put it away. **Three spec rows added mid-build (18, 19, 20)**, all marked as amendments: the progress endpoint, the `EQ` series rule, and enabling the button in replay. **The one criterion that did not pass is AC5** - the chain poll's 3000 ms gap across a scan. A closed market gives one poll, not two, and replay makes **no `dhanPost` calls at all**, so the rate gate is not exercised in this mode even with the market open; what was measured instead is that the scanner only ever uses `scan:quote` / `scan:oi` and that the server answers in max 18 ms throughout a cold scan. **Nine code files**, one over the ~8 guideline. **The live path has never run.** Branch `p8-scanner`. |

| 2026-09-01 | P9 | Option candle colouring. Clicking either half of a strike row switches the existing chart strip into candlestick mode for that one contract - `src/server/candles.ts` computes the whole rule in **one place** over the full 5-day window (`vol >= 3x median20` AND `\|dOI\| >= 5% of oi[i-1]` AND `>= 5 x lot`), renders only the latest session, and never colours the forming candle. `public/candles.js` draws into its **own svg layered inside the same `.chart-body`**, so the two renderers never run at once and neither module imports the other - `body.optmode` plus three `CustomEvent`s carry the whole handover, and app.js still imports nothing downstream of itself. **Verified in replay: 22/22 checks**, zero console errors. Exactly 3 blue and 2 yellow at **1, 5 and 15 minutes alike**, counted both in the payload and off the DOM's own path data; a **second implementation** that recomputes median, thresholds and sign without importing `candles.ts` agrees on all 55 judgeable candles; one blue candle hand-checked end to end (`vol 240,000`, `median20 25,367`, `dOI +280,887`, `oi[i-1] 2,340,724` -> `9.46x` and `12.00%` -> blue); the volume-only candle reads `fired: no`; exactly one in-progress candle, uncoloured; `drawCandles()` **p50 2.20 / p95 3.70 / max 8.00 ms** at 375 candles from eight `<path>` nodes; row 14's 60 s refresh measured at **+60,016 ms** and **0** calls after leaving the mode; 8 screenshots in both themes. **Three spec rows added mid-build (20, 21, 22)**, all marked as amendments: render the latest session only, inject `now` so row 11 is testable at all, and return no candles beyond 15 strikes from the money so row 15's empty state is reachable. **One real bug found by testing:** `app.js` reassigns `className` wholesale on `#chartChg` and `#feedPill`, which silently dropped the `tickonly` class - the underlying's price change and the feed pill stayed on screen beside an option's candle header, reading as that option's numbers. **Seven code files.** **The live path has never run.** Branch `p9-option-candles`. |
| 2026-09-01 | P10 spec | **Spec written, not locked.** `docs/spec/terminal-redesign-v1.md`, 22 rows, emitted for approval; **no `go` came back**, so the file is marked *PROPOSED, NOT LOCKED* in its first line and **zero `src/` and `public/` files were touched**. The row was split into **P10a** (shell + chain) and **P10b** (latency's new form) at **5 code files each**, which is the reason the split exists — the old row carried `~12` against an 8-file rule. The split is cut so that **nothing built in P10a is thrown away in P10b**: P10a leaves the `.lat` right dock working and untouched inside the new shell, and P10b deletes it. The proposal was written against the real screen rather than the requirements, which changed three answers. **The grid already is one scrollable table with the complete strike list and a sticky header** — nothing windows strikes on the server or the client — so "no ATM window, no hidden rows" became an **invariant** (row 8: `tr.hidden` only ever set by an explicit filter, and the header then reads `showing n of N strikes`) rather than a rebuild. **The sticky spine's obvious implementation does nothing here**: the spine is column 13 of 25 at x≈696 and the overflow at 1440px is 44px, so `position:sticky;left:0` would need ~700px of scroll before it ever pinned — it needs `left:0` **and** `right:0`, which pins it in both directions and only conflicts below a 92px scrollport. And the **1484px table does not fit the 1440px floor**, which the P2 deviation row had already priced and handed to P10 — so row 7 hides the eight greek columns behind a `G` toggle, giving **17 columns / 1132px** by default. "TradingView-like" is resolved as **exactly five facts** (row 1) and the latency panel's new form as a **26px always-on rail (zero width cost) plus a closed horizontal drawer (height cost only)**, making the done-when a pixel comparison of `#gridScroll.clientWidth` instead of a judgement. Four risks recorded, the loudest being that `app.js` writes to ~20 ids inside `.lat` and one missing node after the dock is deleted presents as the whole page dying. **Two rows are readings, not measurements, and are flagged as the likely vetoes: row 7 (greeks off by default) and row 11 (dark by default).** Branch `p10-spec-lock`. |


| 2026-09-03 | P10a | Spec **locked with one `go`** — rows 7 and 11, the two the table itself flagged as readings rather than measurements, were put up for veto in isolation and both accepted. Built in `public/panes.js` (new) + four files, exactly the five row 21 costed. The strike spine is sticky on **both** axes: `left:0` alone is inert here, since the spine sits at x≈696 of 1484 and the overflow at 1440px is only 44px, so it would need ~700px of scroll before it engaged. Greeks off by default gives **17 columns / 1132px** and `table.oc scrollWidth === clientWidth === 1440` — the P2 deviation row's 44px debt, paid. The eight `<td>`s stay in the DOM (`display:none` removes a cell from the CSS table but not from `tr.children`), so app.js's `CELL` index constants needed no branch. **Verified in replay: 41/41**, at 1440x900 and 1024x800, zero console errors. **One real bug found by testing**: `skeleton()` built its 25 cells from `COLS` with no `gk` class, so with greeks off — the default, and therefore *every* instrument switch — the shimmer rows generated 25 columns against a 17-`<col>` colgroup (amendment row 24). Two of the first run's failures were the verify script's own: `addInitScript` re-runs on reload and wiped the key it was checking survived, and row 20's 108px belongs to the greeks-**off** case, not the greeks-on one. Branch `p10a-terminal-shell`. |
| 2026-09-03 | P10b | The 380px dock deleted; `public/telemetry.js` (new) owns a 26px always-on rail and a closed-by-default four-column drawer, with **every element id carried over unchanged** so the move is not a rewrite. `#gridScroll.clientWidth` is 1440px with the drawer shut, open, and shut again — the rail costs zero chain width, which is the entire argument for deleting the dock. **Verified in replay: 30/30**, three consecutive runs, zero console errors over 60 s. **Three defects found by testing, all in the new code** (amendment rows 26, 27): the drawer squeezed the chain to **114px** at 1440x900 and **59px** at 1024x800, because row 4's three minimums are geometrically impossible at 1024x800 (~483px of shell for a 140px chart pane, a 140px drawer and a 200px chain, which needs 480 before the splitter) — so they are now ranked, the chart yields to its 70px floor via a `pane-need-room` event and the drawer compresses to 88 before the chain ever loses a pixel; the drawer sized itself once at module load against a layout that had not settled; and **a `ResizeObserver` on `#shell` alone misses the case entirely**, because the shell is `flex:1` of the body, so a chart header wrapping from one line to two grows the chart pane without changing the shell's height — that one failed on about half of runs until `#chartWrap` was observed too. Settled numbers at 1024x800: chart 70, drawer 118.7, chain **exactly 200.0**. `docs/shots/` re-baselined once, at the end, per row 22. |
| 2026-09-03 | P11 | **A full read-only audit of both halves of the tree, at the user's request. 16 defects, every one reproduced with a runnable script before any fix and re-run after.** The two that put wrong numbers on a trading screen: the P9 colour rule was implemented as a **division** where locked rows 6 and 7 are **multiplications**, so a zero baseline inverted it and the two clearest "big player entering" cases the phase exists for — an illiquid strike waking up (`median20 = 0`) and a strike opening fresh (`oi[i-1] = 0`) — could never colour; and `derive.ts`'s `last_price ?? 0` made ATM the **lowest** strike, whose deep-ITM IV (42.5 against a correct 12.1) `BaselineStore` then wrote to `.cache/iv-baseline.json` as the session's IV baseline, wrong for the rest of the day and surviving a restart. The worst by blast radius: `expiry` was **unvalidated** on `/api/stream` and `/api/candles`, and every distinct value left a `ChainPoller` and a `PeakOiStore` listener alive forever — **144 -> 191 MB over 180 unauthenticated requests, linear, retained after every connection closed**; now 400 at the boundary and +1.5 MB. Also: `/api/feed` summed per-connection subscription lists without dedupe (166 and 249 reported for a real set of 83 — and that field is what P5's "zero orphans" criterion is measured through); `feed.ts` never unsubscribed and `REQ` has no unsubscribe code, so Dhan's side only grew toward the documented 5,000 cap, failing *backwards* (old contracts keep streaming, new ones refused) — fixed by reconnecting when the set shrinks, since inventing request codes absent from the contract doc is exactly the guess this project does not make; `scanner.ts`'s `reconciles` was algebraically **always true**, the one thing row 14 exists to catch; a store-wide `onProgress` meant one chip's peak backfill re-rendered every other tab's grid; and a transient failure was cached as a permanent fact in two places. Client: the spot pill was positioned off a `display:none` row and printed the live spot **over the sticky CALLS header**; bare-letter shortcuts fired on modifiers, so **Ctrl+C collapsed the chart and persisted it**; LTP/Volume/OI ticked at 10 Hz while LTP Chg, Vol Chg% and OI Chg stayed on the 3 s poll, so `LTP - LTP Chg` — the previous close, a constant — **drifted 0.81 in 2 s**; `chartPx`/`chartChg` survived an instrument switch, putting NIFTY 50's price under the "NIFTY BANK IDX" label with no tick coming to clear it; `.scan-funnel` hit the P8 `[hidden]` trap again; and one `Esc` closed the scanner **and** tore down the option chart behind it. **12 code files, over the ~8 rule and recorded as such** — it is one audit's findings across the whole tree, not a feature. P10a 41/41, P10b 30/30 and the P2-P9 re-proof 37/37 all re-run green afterwards. |
| 2026-09-17 | P12 prep | **`go` on a blocked phase.** `npm run check` still fails with `DH-901` / `808` (the `.env` token has not changed since it expired on 2026-08-28), so no done-when criterion could be met and **P12 stays blocked**. The credential-free half was built instead. **`npm run live:probe`** (`scripts/live-probe.ts`) makes five serial calls: profile, expiry list, chain, then one `/v2/charts/intraday` on NIFTY's ATM CE and the scanner's real 420-instrument `/v2/marketfeed/quote`. It saves every raw body to `.cache/live/` and prints PASS / CHECK / FAIL per question. The verdicts live in a pure `scripts/live-shape.ts` and re-derive P7's peak, P8's closing OI and the previous session **without** the app's date code (Intl `Asia/Kolkata`, not the `+05:30` arithmetic). **36/36 branches pass** against replay payloads and deliberately broken variants: data-wrapped, millisecond timestamps, OI in contracts, OI in units with the chain in contracts, a missing array, no candles dated today, `net_change` as a percent, `ohlc.close` equal to `last_price`, an absent segment, unrequested ids. Live, only the expired-token gate has run (exit 1, nothing written), plus the no-credentials path (exit 2). **Found in Dhan's docs before any live call:** intraday `fromDate`/`toDate` are documented as `YYYY-MM-DD HH:MM:SS`, while `peakoi.ts` sends date-only. The code was **not** changed on documentation alone; the probe tries the app's form first and retries with the documented one only on `DH-905`. Also from the docs: the quote response carries `oi_day_high` / `oi_day_low` (today's range, not yesterday's peak), and `ohlc.close` is ambiguous. Both endpoints are now written up in `dhan-api-contract.md` §2.5 and §2.6. **One real bug found by running it:** `process.exit()` straight after a `fetch` tripped a libuv assertion on Windows and replaced exit code 1 with a crash code. **3 code files** plus two docs. Branch `p12-live-verify`, commit `bf6cf1b`. |
| 2026-09-17 | P14 | **The user redirected the session from blocked P12 to three new voice notes.** Re-transcribed with whisper (ffmpeg's built-in filter, `large-v3-turbo`): identical to `docs/Recording-14-09-2026.md`, and a restatement of P8 whose one new fact is the data source — "NSE spurt". **Two CLAUDE.md facts overturned by measurement:** nseindia.com *is* reachable from a headed Chrome placed off-screen (200 in ~2.9 s; curl HTTP 000, headless Chromium and headless Chrome `ERR_HTTP2_PROTOCOL_ERROR`), and OI Spurts lists **216** underlyings, not the top 25; its `avgInOI` is the OI change % (216/216). A 12-row proposal got `go` together with a second recording that made the trigger a manual button, N = 20/25/30, and added a pre-defined F&O list. The user's list was validated against four independent sources — NSE `master-quote`, NSE `underlying-information`, NSE OI Spurts, and the Dhan master — **210 = 210 = 210 = 210**, no duplicates. The pasted analysis's "whole market top 20 then cross-match" was measured and rejected: the whole-market top 20 held **zero** F&O stocks that day (all moved 20–38%); the transcript itself says the opposite. NSE's page stops at 20, so N > 20 ranks the full "Securities in F&O" feed, whose local top 20 matched NSE's own lists exactly. Built: `nse.ts` (browser, boundary validation, evidence), `scanner-nse.ts` (pure funnel, counted-at-rejection reconciliation), routes with whitelisted `source`/`n`, panel pickers. **Server 31/31, browser 35/35, all earlier suites green, two real NSE scans (5.4 s, 9.1 s).** Two UI defects found only by reading screenshots (a false "nothing skipped" under the error state — P8 had it too — and a cramped header with misaligned tables). **Collision found mid-verification:** another session in worktree `D:/Temp/Dhan-p13` had taken the name P13 and kept restarting a server on 8787, so part of a regression run measured the other checkout; resolved by messaging it, moving to 8788, renaming this phase P14, and re-running everything against a verified PID. Branch `p14-nse-scanner`. |

## Deviations from the locked spec, and why

| Spec row | Locked | Built | Why |
|---|---|---|---|
| PRD section 6 | Vite + React 18 + TypeScript | Plain ES modules + CSS served by Fastify; Node 22.6+ runs the server TypeScript natively | No bundler means no build step and no build-failure surface. One command runs everything. The token sheet and grid rules are unchanged. |
| Row 8 | Panel always docked right at 380px | Panel docked right at 380px but **closed by default below 1780px viewport**, with a live RTT/age readout kept in the top bar | At 1440px an open panel forces the 23 columns to scroll sideways. The user asked for the strike to be clearly visible with the grid neat and clean, which the full-width grid delivers; latency stays visible in the top bar either way. |
| Row 9 | Sparkline shows last 60 calls | Same, plus a `collecting - n of 2 calls` placeholder | An empty chart on a closed market read as broken. |
| Row 8 (again) | 23 columns fit at 1440px with the panel closed | **25 columns, table min-width 1484px**, so the grid scrolls ~44px sideways at 1440px | `peak-oi-v1.md` row 20 priced this before it was built and offered `Vol Chg%` as the trade. P10 owns the final column layout; until then the cost is 44px of horizontal scroll on the narrowest supported screen. |
| scanner-v1 row 9 | The `Scan` button is enabled whenever the NSE equity session is open | Enabled when the session is open **or** `REPLAY=1` | Row 9 as written makes P8 untestable outside 09:15-15:30 IST, and almost all work on this project happens outside it. Replay data is synthetic and has no session, so gating it on a real session gates it on nothing. `scanner-v1.md` row 20. |
| option-candles-v1 row 11 | The in-progress candle is plain green/red, re-evaluated when it closes | Same, but `now` is an **argument** to the rule, not `Date.now()` | On a closed market — almost every session on this project — no seeded candle is ever still forming, so the branch is unreachable and AC4 passes vacuously. Replay injects the last seeded candle's open + 1 s. `option-candles-v1.md` row 21. |
| option-candles-v1 rows 3, 18 | Fetch 5 calendar days; the seeded replay day has 75 candles | Fetch 5 days, **draw only the latest session**; the earlier days feed `median20` and are dropped | Nothing in the locked table said which of the five days is on screen. Drawing all of them puts 375 candles on a 5-minute chart and makes row 18's count ambiguous. `option-candles-v1.md` row 20. |
| terminal-redesign row 13 / out-of-scope | The rail lists conn, mode badge and clock; the out-of-scope line forbids topbar changes beyond removing `#latMini` | The conn dot, mode badge and clock **move** from the topbar into the rail, ids unchanged; `#feedPill` / `#tickRate` move out of the chart header and lose `tickonly` | The two instructions contradict each other, and row 15's own stated reason — *"two places showing the same number is how they come to disagree"* — settles it for moving rather than duplicating. `tickonly` existed because P9 found the feed pill reading as an option's numbers **beside an option's candles**; in a global rail under every pane that ambiguity does not exist. `terminal-redesign-v1.md` row 25 |
| terminal-redesign row 4 | Chart min 70, drawer min 140, chain min 200 | The three are **ranked**: chart yields to 70 first, then the drawer compresses to a floor of 88, and the chain's 200px is never broken | They are geometrically impossible together at 1024x800 — ~483px of shell for what needs 480 before the splitter. Measured, not predicted: the first build gave a 114px chain at 1440x900 and 59px at 1024x800. On a trading screen the chain is not the one that loses. `terminal-redesign-v1.md` row 26 |
| option-candles rows 6, 7 | `vol >= 3.0 * median20` and `abs(dOI) >= 0.05 * oi[i-1]` | **No deviation — the code was wrong and P11 corrected it.** It had been implemented as `vol / median20 >= 3.0` with a `median20 > 0` guard, and the same for OI | Recorded here because it looked like a deviation for two phases and was not one: a division plus a `> 0` guard inverts the answer on a zero baseline, so the two clearest cases the phase exists for were silently dropped. P9's hand-checked candle still fires blue after the correction |
| P5 replay feed | Contract ticks carry a plausible OI | OI is **seeded from the snapshot and random-walked ±0.2%/tick**; the chain's OI drift is proportional to each contract's own OI rather than a flat 900/poll | The feed drew `1e5 + rand*4e6` fresh for every contract on every tick — a strike's OI teleported between 1 L and 41 L ten times a second, unrelated to the chain. Nothing compared OI to anything before P7, so it was invisible. `peak-oi-v1.md` row 22. |
